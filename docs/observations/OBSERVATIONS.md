# OBSERVATIONS — the ledger

> **Seeded 2026-08-12 under `016` §5b.** One row per finding. `tests/test_observations_ledger.py`
> goes **RED while any row is `OPEN` past its `review-by` date** — red for being *ignored*,
> not for being open.

**Every row cites where it came from. A finding with no source does not go in.**

**`kind` separates an OBSERVATION from a READING.** An observation is something measured. A
reading is an inference about what produced it. Conflating the two is how a plausible
explanation becomes a recorded fact — `012` came within one sentence of recording Cboe One
odd-lot filtering as the established cause of a 5.32× discrepancy, and it is not established.

**Status is `OPEN` · `PROMOTED` · `DROPPED`.** `PROMOTED` and `DROPPED` both require a
`resolution:` line in the Resolutions section naming where it went or why it did not.
**Deleting a row does not clear it** — see the test.

**Nothing here has been acted on. Recording is not acting**, and `016` forbids acting on any
seeded row.

---

## Why `review-by` is 2026-11-12

**Three months from seeding.** The interval is a property of what these rows are, not a round
number:

- **Almost every row is blocked on a slice that has not been built.** Rows 1–4 are questions
  about tape structure that `S012`/`S016` will answer as a side effect; row 5 needs a
  `BUILD-PLAN` revision; rows 9–10 need `S008`. `BUILD-PLAN.md` §1 sizes a slice at one to two
  Claude Code sessions and core alone is four slices. **A review date that falls before the
  work that would settle these has arrived converts the ledger into a recurring interruption**,
  and a test that cries wolf is a test people learn to make green rather than read.
- **It must be shorter than the memory of why the row exists.** Three months is inside the
  span of this project's own git history, so a reviewer can still reconstruct the context.
- **It is a floor, not a schedule.** Any row can be resolved the day its slice lands; the date
  only says *by when someone must have looked*.

**All twelve share one date deliberately.** Staggering them would be a guess dressed as
precision — nothing here justifies claiming row 7 needs attention sooner than row 8.

---

## The ledger

| id | date | kind | what was seen | what produced it | what would settle it | status | review-by |
|---|---|---|---|---|---|---|---|
| **OBS-001** | 2026-08-11 | OBSERVATION | **88.9 % of all prints in the capture are odd lots**, mean 47.5 shares/print. Any per-print statistic on this tape is dominated by odd lots | `handoff/done/012-live-qqq-tape-capture.md` | An independent source for QQQ odd-lot share on 2026-08-11. Nothing in the capture cross-checks it — `012` says so explicitly | OPEN | 2026-11-12 |
| **OBS-002** | 2026-08-11 | OBSERVATION | **`FINRA` is 57.88 % of prints and 42.57 % of shares.** Off-exchange prints are IN `whatToShow="TRADES"`, and are the largest single component | `handoff/done/012-live-qqq-tape-capture.md` — discharges `008a` Test 5 | Already answered decisively for tick-by-tick. Open only for whether IBKR's *historical* endpoint filters differently, which its documentation implies and nothing here tests | OPEN | 2026-11-12 |
| **OBS-003** | 2026-08-11 | OBSERVATION | **`CHX` is 0.11 % of prints but 3.82 % of shares** — 1,606 shares/print, **34× the overall mean.** One venue carries block-sized prints and almost nothing else | `handoff/done/012-live-qqq-tape-capture.md` | Whether it holds across sessions and symbols. One session on one symbol cannot distinguish a venue's character from a day's accident | OPEN | 2026-11-12 |
| **OBS-004** | 2026-08-11 | OBSERVATION | **The capture is 5.32× larger than TradingView's same-window volume** — 873,482 shares against 164,280 | `handoff/done/012-live-qqq-tape-capture.md` | TradingView's venue list for that session. The magnitude is measured; the cause is not | OPEN | 2026-11-12 |
| **OBS-005** | 2026-08-11 | **READING** | **Cboe One (four lit venues, ~25 % of tape) plus odd-lot filtering is the most probable cause of OBS-004.** Consistent with direction and roughly with magnitude. **NOT ESTABLISHED** | `handoff/done/012-live-qqq-tape-capture.md`, which states it as a reading | Same as OBS-004. Recorded separately from it **on purpose**: the measurement stands whether or not this explanation does | OPEN | 2026-11-12 |
| **OBS-006** | 2026-08-12 | OBSERVATION | **`manage` has no slice anywhere in `BUILD-PLAN.md` §3–4.** Reconciliation appears inside `S017`'s pre-send checks and `S015`'s execution pull; nothing builds managing an open position | `S009a` session output, from declaring the twelve stages | A `BUILD-PLAN` revision that either assigns the stage or states that it is deliberately out of scope. **It renders `[ NOT BUILT ] (slice not assigned)` on screen today** | OPEN | 2026-11-12 |
| **OBS-007** | 2026-08-11 | OBSERVATION | **`SPEC.md` §4d names `SSH_CONNECTION` as the ASCII-fallback trigger. The real property is whether the output encoding can carry the box characters** — a Windows console on cp1252 dies on `┌` with no SSH involved | `handoff/done/S009-tui-frame-and-refusal-grammar.md` | A `SPEC.md` §4d amendment. The code already implements the wider trigger; the spec still names the narrow one, so the two disagree | OPEN | 2026-11-12 |
| **OBS-008** | 2026-08-11 | OBSERVATION | **The adoption gate has no route for natively-authored new code.** `BOOTSTRAP_ALLOWLIST` is doing two jobs and grows ~11 entries per slice | `handoff/done/S009-tui-frame-and-refusal-grammar.md` | A decision on whether authored-here files get their own route or keep using the allowlist. **The growth rate is the evidence** — at 11/slice the allowlist stops being a list of exceptions | OPEN | 2026-11-12 |
| **OBS-009** | 2026-08-11 | OBSERVATION | **`H9-v3-specs-into-new-tree.md` was never carried into this tree; H9 was built from v2** | `handoff/done/013a-*.md` | Reading v3 against what H9 actually built. Until then it is unknown whether v3 asked for anything v2 did not | OPEN | 2026-11-12 |
| **OBS-010** | 2026-08-11 | OBSERVATION | **`condition_codes.yaml` needs rewriting, not deleting** — its banner asserts ITCH provenance it does not have | `handoff/done/013c-*.md` | Identifying the vocabulary's actual source. **Deleting it would lose the fact that something once claimed ITCH provenance**, which is the finding | OPEN | 2026-11-12 |
| **OBS-011** | 2026-08-11 | OBSERVATION | **The separation guard misclassifies `OMCL 2024-08-01` and `ITCI 2025-01-10`.** Latent until the identification window widens past 15 s | `handoff/done/013c-*.md` | Widening the window in a test and confirming both flip. **Latent is not fixed** — it is a defect with a trigger nobody has pulled yet | OPEN | 2026-11-12 |
| **OBS-012** | 2026-08-11 | OBSERVATION | **`git ls-files` reads the index and reports staged files as present.** `git cat-file -e HEAD:<path>` is the check that answers *"committed"* | `handoff/done/013b-*.md` | Nothing — this is settled fact about git. **It stays OPEN because the tree has not been audited for the wrong check.** `tests/test_no_secrets.py::test_claude_config_is_not_tracked` uses `git ls-files` today | OPEN | 2026-11-12 |
| **OBS-014** | 2026-08-12 | OBSERVATION | **The too-small guard was evaluated once, at launch.** Launched below the per-tile minimum it refused correctly; shrunk after launch it never fired, and panels truncated to `WATCHLIS...` and `(no wat...` instead | UAT `christoph/done/009-s009a-read-the-screen-at-working-width.md`, answers B and C | **SETTLED.** See resolution | PROMOTED | 2026-11-12 |
| **OBS-015** | 2026-08-12 | OBSERVATION | **Pipeline rows 5 and 8 rendered with an empty name column.** Both were declared `name: "[HUMAN]"` while the value cell already said *your decision*, so the name read as a gap rather than a stage | UAT `christoph/done/009-s009a-read-the-screen-at-working-width.md`, answer D | **SETTLED.** See resolution | PROMOTED | 2026-11-12 |
| **OBS-016** | 2026-08-12 | OBSERVATION | **`manage` rendered `[ NOT BUILT ] (slice not assigned)` after it had been ruled deferred.** *Nobody decided* and *decided to postpone* carry different weight, and the screen was making the weaker one | `018` part 4, from Christoph's ruling of 2026-08-12. **The underlying gap OBS-006 records is unchanged** | **SETTLED for the rendering.** OBS-006 stays OPEN | PROMOTED | 2026-11-12 |
| **OBS-017** | 2026-08-12 | OBSERVATION | **The captured ARCA depth arrays are NOT price-ordered — 99.9 % of bid arrays and 99.7 % of ask arrays, measured over 20,000 snapshots.** The array index is the DOM row position, preserved exactly; nothing sorts or normalises. **The book is fully reconstructable.** What is missing is self-description: no `position` key, no schema note, so `bids[0]` reads as the best bid and is wrong almost always | `018` part 1, from `tools/capture_tape.py:231-242` and `records/tape/QQQ-2026-08-12-depth.jsonl`. Confirms `S009a`'s live probe, whose cause was recorded as unestablished | **The diagnosis is settled.** What is open is the DECISION: a `position` key for future captures, a documented schema covering the 4,324,111 records already held, or neither. Christoph's call | OPEN | 2026-11-12 |
| **OBS-018** | 2026-08-13 | OBSERVATION | **The Drive export carries `.md` only, so two files in `handoff/` cannot reach the design session by export** — `A1-connector-from-scheduled-run.txt` and `accepted/.gitkeep`. 76 of the 78 files in `handoff/` exported. **The `.txt` is a real handoff artifact**, and the whole point of the export is that a file the design session cannot see did not happen | `handoff/done/020-drive-export-of-handoff-and-christoph-done.md`, from the first real export at `a950ae1` | A decision: widen the export beyond `.md`, convert the artifact to `.md`, or rule that `handoff/` is markdown-only and the `.txt` is misfiled. **Not urgent — each manifest names every file it skipped**, so the omission is visible rather than silent, which is the property that makes this an observation and not a defect | OPEN | 2026-11-13 |
| **OBS-019** | 2026-08-13 | OBSERVATION | **The rotated Databento key still sits in cleartext in Claude Code's machine-local caches** — 39 files under `~/.claude/file-history/`, 2 session transcripts under `~/.claude/projects/`, and `~/.claude/history.jsonl`. **Nothing is in any git history** (0 occurrences in `momentum`, `momentum-harness` and `tws_order`) and `D:\Dev` is clean. The key was rotated 2026-08-10, so these are inert | `handoff/done/022-for-code-secrets-hygiene.md`, from the 022 search | **A decision, not a measurement.** Purging them is Christoph's call: `file-history/` backs Claude Code's undo, and `projects/` is the session record. **The same reasoning that ruled out rewriting git history applies — rotation was the fix.** Deliberately not deleted | OPEN | 2026-11-13 |
| **OBS-020** | 2026-08-13 | OBSERVATION | **The user-level `~/.claude` is not scanned by `test_no_secrets.py`, and that is a deliberate narrowing with a real blind spot.** Excluded because it is per-user rather than adjacent to this repo, holds session transcripts of arbitrary pasted text so the shape patterns fire on prose, and is gigabytes — the exact runtime failure `records/` caused once. **A credential there is not caught by the suite**; OBS-019 shows one is | `tests/test_no_secrets.py::candidate_roots` docstring, and `test_the_parent_directory_claude_is_a_candidate_root` which pins the exclusion | A separate reporting tool — the architect design for 022 proposed `tools/scan_credential_surfaces.py` as `verify.ps1` section 6, printing every surface including `~/.claude`, env vars and shell history, with hits redacted. **Not built under 022** — out of scope for a task scoped to the tree. **The header prints the gap on every run**, which is what keeps it an observation rather than a hole | OPEN | 2026-11-13 |
| **OBS-021** | 2026-08-13 | OBSERVATION | **A permission allow-rule in `D:\Dev\.claude\settings.local.json` contains an entire pasted paragraph of design prose**, captured into what should be a `Bash(grep ...)` pattern. It is one of 105 rules. **A permission rule can silently hold arbitrary text**, and nobody reads a 105-entry allow list | The 022 search, which read the file in full to remove three key-bearing rules | Whether any rule in that file authorises more than its author intended. **Not audited under 022** — the task's scope was the credential, and widening it silently is the failure this project keeps naming. The file is outside every repo, so no test covers it beyond the credential scan | OPEN | 2026-11-13 |
| **OBS-022** | 2026-08-13 | OBSERVATION | **`test_shrinking_after_launch_refuses_and_growing_back_restores` failed once and then passed on every subsequent run.** It failed in one whole-suite run, passed in isolation immediately after, and passed in the next two whole-suite runs. **It is a Textual `run_test` pilot whose only synchronisation after `resize_terminal` is a single `await pilot.pause()`** — one scheduler turn, under whatever load the rest of the suite is generating | 026's suite runs, 2026-08-13. `live/tests/test_tui_measured_against_its_tile.py` | Replacing the bare `pause()` with a condition wait, then running the suite N times and counting. **Until then no run of this suite is fully trustworthy in either direction** — this test guards the guard that shipped broken once, so a false green here is the expensive failure, and a false red teaches people to re-run until green. Not fixed under 026, which does not own this file | OPEN | 2026-11-13 |
| **OBS-023** | 2026-08-13 | OBSERVATION | **HYG pre-market credit was unreadable at 05:00 — 0 shares traded through 05:15 ET, below the 25,000-share floor. 4 consecutive sessions.** **CLOSED HISTORY, NOT A LIVE BLINDNESS** — the consumer it disabled no longer exists | The 2026-08-13 snapshot's `could_not_do` text via `027`. **Corrected by `028` Part 1** | **SETTLED.** See resolution | DROPPED | 2026-11-13 |
| **OBS-029** | 2026-08-13 | OBSERVATION | **HY OAS unreachable at FRED, twice — below rule 15's three-session threshold, so `027` recorded it as a watch rather than a row.** **There is nothing to watch:** Layer I row 1 was cut in prompt v1.7 and replaced by *credit, prior session* from IBKR daily bars | `027`'s done-note, §2 "HY OAS — confirmed at 2 of 3"; retired by `028` Part 1 | **SETTLED.** See resolution | DROPPED | 2026-11-13 |
| **OBS-024** | 2026-08-13 | OBSERVATION | **No market-wide pre-market gap-breadth source exists in this toolset at any hour**, and the 07:00–09:30 window is not open at read time either. 4th consecutive session. **Disabled consumer: Strip row 10 AND veto 4** | As OBS-023 | A wired breadth source, or a decision that row 10 is a 09:30 measurement rather than an 05:00 one. **Two failures in one row** — no source at all, and the window being shut — and fixing only the second leaves it dead | OPEN | 2026-11-13 |
| **OBS-025** | 2026-08-13 | OBSERVATION | **VIX-family failure, a DIFFERENT MODE each session** — snapshot empty, then history errored, then an `is_close` flag. 4th consecutive session. **Disabled consumer: Strip row 1's ±3% legs** | As OBS-023 | **The varying mode is the finding.** One source failing one way is a supply problem; one source failing three ways in four sessions is an integration that has never worked, and a fix aimed at any single mode will look successful and change nothing | OPEN | 2026-11-13 |
| **OBS-026** | 2026-08-13 | OBSERVATION | **COR1M 6-month percentile unavailable — dispersion. 5th consecutive session**, the longest-running of the five. **Disabled consumer: state-machine rule 2, EVERY SESSION** | As OBS-023 | A dispersion source, or an amendment to state-machine rule 2 that does not require COR1M. **Rule 2 has never once been exercised**, so nothing is known about whether it works — this is not a degraded input, it is an untested rule | OPEN | 2026-11-13 |
| **OBS-027** | 2026-08-13 | OBSERVATION | **NYSE up/down volume and % of S&P 500 above 20DMA both unavailable. 4th consecutive session.** **Disabled consumer: the Layer I breadth row is half-unsourced** — it renders from the half that resolves | As OBS-023 | Either source wired, or the row declaring itself partial. **A row that renders from half its inputs and says nothing is the failure this project names most often**: a well-formed value answering a narrower question | OPEN | 2026-11-13 |
| **OBS-028** | 2026-08-13 | OBSERVATION | **Rule 15 cannot be made mechanical against the current snapshot format, for two independent reasons.** (1) `could_not_do` is a **list of free-text strings** in `REGIME-PROMPT.md` — no `id`, no key — and the 08-13 entries embed that session's numbers, so exact matching cannot group them across sessions. (2) **`docs/regime-snapshots/` contains zero snapshots**, so any such test passes vacuously today | `docs/specs/REGIME-PROMPT.md` §1 schema block; `ls docs/regime-snapshots/` → `.gitkeep` only. 027 part 2 | **A requirement on `REGIME-PROMPT.md`: each `could_not_do` entry must carry a stable `id`**, unchanged across sessions, with the session-specific numbers in a separate field. 027 says the design session will amend the prompt. **No heuristic was built** — a matcher that silently mis-groups is worse than no test, because its green would mean nothing | OPEN | 2026-11-13 |
| **OBS-030** | 2026-08-13 | OBSERVATION | **Nothing watches whether an authored document reached the thing that runs it.** `REGIME-PROMPT.md` **v1.7 was authored on 2026-08-13 and never pushed to the scheduled task's trigger — it was live nowhere**, while asserting in its own §0 that it *was* the stored prompt. It passed every check available to it. The version pin compares the TREE copy to the AUTHORED document; **no check compares the authored document to the DEPLOYED one**. **Added by `030`: the tree copy sat at v1.2 through six versions and nothing went red, because the pin compares the file against a version number a human typed into the test, and reads the file's own claim about itself for the other half — a pin that asks its subject what version it is cannot notice that the subject is stale. That is the self-reference trap (§7), and it is why six versions were invisible** | `028` Part 2, from the design session reading the trigger's stored prompt rather than trusting the document; extended by `030` | **A gap with no owner, and that is the finding.** Neither side can close it: this session cannot read the trigger and neither can the tree. It needs either a trigger that publishes its prompt version somewhere both can see, or a person who checks. **`028` explicitly forbids building it here** — a check that cannot reach the thing it checks would be theatre | OPEN | 2026-11-13 |
| **OBS-031** | 2026-08-13 | OBSERVATION | **A mutable state cannot live in an immutable file, and the handoff convention now requires both.** The five states describe a handoff that PROGRESSES; the Drive channel makes a task file IMMUTABLE — `026`'s copier refuses to overwrite one that differs. **`**Status** WRITTEN` is true when written and wrong within the hour**, and correcting it in the inbox copy makes that file differ from its Drive original forever. Task files `021`–`027` carry `status: READY` frontmatter instead of the header, and `test_every_task_file_declares_a_state` has been red on every run since | `028` Part 3a; `handoff/inbox/021`–`027`; the live `026` conflict, which is this failure already happening once | **Christoph's decision, and `028` records the recommendation without implementing it:** the header becomes a **provenance stamp** — the state at authorship, never updated — and the live state moves to a ledger the protocol document owns. **Backfilling `021`–`027` is explicitly the wrong fix**: it would make seven files differ from their Drive originals and the copier would report seven conflicts on every run, forever | OPEN | 2026-11-13 |
| **OBS-032** | 2026-08-13 | OBSERVATION | **The suite tests `live/` as a library; the UAT tests it as a program.** 236 tests passed while `python -m live.tui.app` exited silently — no output, no traceback, exit 0 — because `app.py` defined `MomentumApp` and nothing ever called `.run()`. Every test in `live/tests/` drives the app through Textual's `run_test()` pilot, which constructs the class directly and **never needs a process entry point**. **Third green suite over a broken `live/`** | `029`, from Christoph's `013` UAT attempt; `live/tui/app.py` before this task | **The general case is NOT settled.** `029` adds one subprocess launch test for the one command that exists today and **forbids generalising beyond it**. What is open is the rule: **every slice that adds a user-facing command needs a launch test, not only a pilot test** — nothing enforces that, and the next command will arrive by the same route this one did | OPEN | 2026-11-13 |
| **OBS-033** | 2026-08-13 | OBSERVATION | **One carried evidence file's recorded hash can never be reproduced by a checkout.** `docs/observations/session-defined-twice.md` is recorded in `EVIDENCE-CARRY.md` as `77ef9f79…`, which is the **CRLF** copy sitting in `D:\Dev\momentum` (3,012 bytes, 64 CRLF pairs). `.gitattributes` pins `* text=auto eol=lf`, so git writes **LF** — 2,948 bytes, `b90faf85…`. **`test_no_carried_file_has_been_modified` is therefore red in any fresh clone or worktree and green only in the one working copy that predates the attribute.** The other carried files all match; this is a single-file anomaly | `029`, from a full-suite run in a git worktree — the failure named exactly this file and the two hashes were compared directly | **A decision, not a repair, and the wrong fix is obvious and tempting.** Re-recording the hash from the LF copy would make an evidence ledger agree with a file that was *normalised after it was carried* — the carry rule forbids reformatting evidence, and the CRLF bytes are what was carried. The choices are: record both hashes, add a `-text` attribute pinning this file's bytes, or rule that the ledger hashes normalised content. **Nothing was changed** — the discrepancy is 64 line endings and no content | OPEN | 2026-11-13 |
| **OBS-034** | 2026-08-13 | OBSERVATION | **A git worktree under `.claude/worktrees/` turns two tests red in the main checkout, and they go red for a COPY of the repo rather than for anything in it.** `test_every_directory_holding_tests_is_declared` and `test_no_legacy_regime_snapshot_path` walk the filesystem from the repo root; a worktree is a second full checkout inside that root, so every `test_*.py` — and every historical mention of the legacy snapshot path the second test forbids — is counted twice. Both name real-looking paths under `.claude/worktrees/`, which read as live breakage. **Writing this row proved the point a second time**: quoting the forbidden literal here turned that same test red on the ledger, so the row describes the string instead of spelling it | `029`, from working in a worktree created by `EnterWorktree` and then running the suite in `D:\Dev\momentum` | **Not fixed here, deliberately.** Both tests carry an explicit *DO NOT ADD A PREFIX TO AN EXEMPTION LIST* warning, and `test_no_legacy_regime_snapshot_path`'s exemption is derived rather than listed — widening either to skip `.claude/` is the exact move they forbid. **It is also transient**: removing the worktree clears it. What is open is whether a nested checkout should be invisible to a filesystem walk **structurally** — e.g. deriving the scan root from `git ls-files` instead of `Path.rglob` — which is a change to how every scanning test works and is not `029`'s to make | OPEN | 2026-11-13 |
| **OBS-035** | 2026-08-13 | OBSERVATION | **Three features have shipped complete, tested and unreachable** — the app with no entry point (`029`), `attach()` with nothing calling it (`032`), and the palette that existed only in a docstring. **The suite tests every piece as a library; only a UAT has ever tested the program.** `live/tests/test_attach.py` exercised `attach()` in 395 lines while `live/tui/app.py` imported three local modules and none of them was `live.attach` — so 288 tests were green against a terminal with no way to attach a symbol. **This is the class, not the instance**: `029` closed the entry-point case and `032` closes the attach case, and neither closes the next one | `032`, from Christoph launching the terminal on 2026-08-13 and asking how to attach QQQ — **the question is the finding**; `029` for the first instance | **A rule nothing enforces: every slice that adds something a person must reach needs a test that reaches it the way a person would — a key press, not a method call.** `029` added a launch test, which proves the program *starts* and nothing about what it can *do*; `032` adds a key-press test for one binding. **Neither generalises, and both were written only after the feature had already shipped unreachable.** What would settle it is a check that fails when a slice adds a user-facing surface with no test that drives it through real input dispatch — which nobody has designed | OPEN | 2026-11-13 |
| **OBS-036** | 2026-08-13 | OBSERVATION | **Demonstrating a test red by reverting files in a shared checkout can destroy another session's uncommitted work, and the destruction would be invisible.** Two sessions worked `032` in `D:\Dev\momentum` simultaneously on 2026-08-13. One demonstrated its red by running `git show HEAD:live/tui/app.py > live/tui/app.py` in the shared tree; the other did the same demonstration in a **detached worktree at `HEAD`** (`git worktree add --detach`) and never touched the shared file. **Both methods produce the same evidence; only one can silently overwrite a concurrent writer.** No harm occurred here — settled by reading the other session's transcript, not by inference: its only uncommitted `app.py` edit was a scaffold it added at 17:27:37 and removed itself at 17:27:53, ninety seconds before the overwrite at ~17:29:20 | `032`, from the collision itself; the other session's transcript at `~/.claude/projects/D--Dev-momentum/`, which is the only record that could answer it | **`031` is the general fix and is unbuilt.** What this row adds is narrower and immediately actionable: **the red demonstration for any test should be run in a throwaway worktree, never by mutating the shared tree.** It costs one `git worktree add` and removes the whole failure mode. **Note what made this answerable at all** — a transcript on disk, not a mechanism in the repo. Had the other session been on another machine, the honest answer would have been *unknown*, and *unknown* is the normal case | OPEN | 2026-11-13 |
| **OBS-037** | 2026-08-13 | OBSERVATION | **A test that asserts an absence passes against a feature that does not exist, and two independently-written suites each contained exactly one — neither author noticed.** `032` was built twice in parallel by two sessions. Session A's suite of 8 had one test green against a binding no key reached: an import assertion, *does `app.py` import `live.attach.attach`*. Session B's suite of 11 had one green for the same reason: `test_an_empty_submit_attaches_nothing_and_closes` — *press the key, submit nothing, panel unchanged* — **because a panel nothing can change is trivially a panel nothing changed.** Two authors, no contact, same defect shape, one instance each | `handoff/done/032-for-code-attach-is-unreachable-from-the-tui.md` §2, from both sessions' red runs against pre-032 `HEAD` `4236cd0` | **A rule, and it is the on-screen `[ NOT BUILT ]`-versus-data-absent distinction turned inward.** The terminal already refuses to render *"the machinery does not exist"* and *"the machinery exists and the input is missing"* identically; **its own tests do exactly that and nothing checks.** What would settle it: whether every absence-asserting test in this repo is paired with a positive test that fails when the mechanism is removed — a negative assertion is only meaningful next to a positive one that can go red. **Not swept here** — `032` owns one file, and auditing the suite is not its scope | OPEN | 2026-11-13 |
| **OBS-038** | 2026-08-13 | OBSERVATION | **The subagent roster's central restriction cannot be fully enforced, and the roster says so in the one place it matters.** `024` builds four agents whose roles are separated by TOOL LIST rather than by instruction — *a reviewer with no `Edit` tool cannot edit*. **That holds for `Write`/`Edit` and is false for `Bash`**: the reviewer has `Bash` (it cannot run the suite otherwise) and `echo > file` is one keystroke away. Same for `test-author`, which holds `Write`/`Edit` because it must create test files, so *"only in `tests/`"* is a convention with nothing behind it. **Two of the four agents therefore carry a prohibition their own capabilities let them break** — which `016` part 7 and `020` Refusal A both say must never be asked of a party | `024`; the tool lists in `.claude/agents/*.md`; `tests/test_subagent_roster.py::test_bash_is_write_capable_and_the_roster_says_so`, which asserts the weakness is *documented* rather than asserting it is absent | **A `PreToolUse` hook that refuses mutating shell commands per-agent, and a path-scoped write guard for `test-author`. Neither exists.** Until then the honest position is the one the definitions take: label the unenforceable prohibitions as conventions so they do not sit beside the enforced ones borrowing their authority. **Do not close this by deleting the `Bash` grant** — a reviewer that cannot reproduce a defect is guessing, and that trade was made knowingly | OPEN | 2026-11-13 |
| **OBS-039** | 2026-08-13 | OBSERVATION | **`config/sync.yaml` pins its destination to an absolute path in the main checkout**, `D:\Dev\momentum\docs\regime-snapshots`, so `test_the_destination_paths_are_inside_the_repo` **fails in any worktree** — the destination is genuinely outside the tree the test is running in. Found incidentally by running the full suite from a worktree under `024`; **it is `025`'s to deal with**, since `025` is the task that first exercises the regime pair `026` configured but never ran | `024`, from `pytest` in `.claude/worktrees/024-subagent-roster`; `config/sync.yaml`, the `regime_snapshots` entry | **Whether a sync destination should be repo-relative rather than absolute.** An absolute path is arguably correct for a machine-local pipeline and arguably wrong for a value in a committed config — it means the config describes one checkout on one machine. **Not decided here, and `024` deliberately did not touch it**: changing a `025` input from inside `024` is how a task acquires work nobody scoped | OPEN | 2026-11-13 |
| **OBS-040** | 2026-08-13 | OBSERVATION | **A `socket.socket.connect` guard does not stop an asyncio client, and this repo has one that is trusted to.** `034`'s launch guard patched exactly that method — copying the pattern from `tests/test_keepuptodate_scale.py` — and the launched app **still connected to live TWS**, rendering `connected · client 7 · read-only` on the screen the test was asserting against. On Windows `asyncio` runs a `ProactorEventLoop` and `loop.create_connection` reaches the socket through `ConnectEx`, never calling the patched method; `ib_async` is asyncio-native, so **every connection it makes takes the unguarded path.** Fixed in `034` by also patching `asyncio.base_events.BaseEventLoop.create_connection` | `handoff/done/034-for-code-wire-the-broker-into-main.md`; the guard in `live/tests/test_launches_as_a_program.py`; the unfixed one in `tests/test_keepuptodate_scale.py::test_importing_the_probe_connects_to_no_broker_port` | **Whether `021`'s guard still guards anything.** It catches the incident it was written for — a *synchronous* connect at import — and would pass against a module that dialled 7496 through asyncio, which is how every module in this tree now talks to IBKR. **Not fixed there by `034`**: changing another task's test from inside this one is how a task acquires work nobody scoped. What would settle it: the same two-patch guard applied there, seen red against a planted `connectAsync` | OPEN | 2026-11-13 |
| **OBS-041** | 2026-08-13 | OBSERVATION | **The attach blocks the UI thread for the length of its historical requests** — a live QQQ attach measured **13.7 s wall-clock**, during which the terminal repaints nothing. `ib_async`'s sync API raises `RuntimeError: This event loop is already running` inside Textual, so `034` gave the client its own loop on its own thread and marshals each call across, which preserves the synchronous `MarketData` Protocol **and blocks the caller by construction** | `handoff/done/034-for-code-wire-the-broker-into-main.md`; `live/attach/ibkr.py`, `_BrokerLoop.call` | **Moving the attach to a Textual thread worker.** Not done in `034`, which required the attach binding unchanged, and the change is not cosmetic: the existing pilot tests synchronise on `pilot.pause()`, which does not wait for a worker. **`request_timeout_s` bounds the freeze rather than removing it**, and raising it to 60 s (see OBS-042's sibling finding on RVOL) lengthens the worst case by the same amount. A terminal that has stopped repainting is indistinguishable from one that has crashed | OPEN | 2026-11-13 |
| **OBS-042** | 2026-08-13 | OBSERVATION | **The context block is 26 rows and the `ATTACHED` tile is given 10–12 at 209×54, so the entire level rail renders below the fold — and nothing in the program can scroll it.** `Panel` is a `Static` that renders `self.rows[:viewport]`; `Ctrl+Tab` rotates focus and no key scrolls. §4e's `+16 more ↓` is therefore honest about the loss and **the hidden rows are unreachable rather than merely off-screen.** PDH/PDL, PMH/PML, ORH/ORL, 52wH/52wL and the round-number count are measured, carried on the day record, and cannot be seen from the running terminal | `handoff/done/034-for-code-wire-the-broker-into-main.md`, from the live QQQ attach at 13:24 ET; `live/tui/app.py`, `Panel.body` | **Whether the sixth instance of *computed and unreachable* is the tile or the mechanism.** Two shapes: give `Panel` a scroll, or stop putting the rail in a tile that cannot hold it. **`christoph/open/013` compares the context block against his charts and the rail is half of what he would compare** — so this gates the UAT rather than merely degrading it. Not decided in `034`, which was forbidden from changing the tiling | OPEN | 2026-11-13 |
| **OBS-043** | 2026-08-13 | OBSERVATION | **`Measured` has no as-of and no lag field, so `SPEC.md` §3's *source, as-of, lag on every value* cannot be rendered per value.** It carries `value`, `sample` and `unavailable`; `sample` is the window a number was computed over — `20 sessions, excl. today` — which is provenance but is not a time. `034` renders **one block-level stamp** (`from IBKR 127.0.0.1:7496 · as of 13:24 · lag 35s`) derived from the newest session bar, and each row carries its own `sample` | `handoff/done/034-for-code-wire-the-broker-into-main.md`; `core/indicators/context.py`, the `Measured` dataclass | **Whether per-value stamping is wanted, and what it would cost.** Every row of one attach comes from one of four requests, so a per-value as-of would be four distinct times, not twenty-six — the honest unit may be the REQUEST rather than the value. **Deliberately not stamped at render time**: a timestamp taken when the screen is painted says when the screen was painted, which is a well-formed value answering a different question | OPEN | 2026-11-13 |
| **OBS-044** | 2026-08-14 | OBSERVATION | **The inbound copier has the exact defect `037` was raised to fix in the outbound one, and `037` forbade touching it.** `export-handoff.ps1` left its only evidence of running *inside the destination* and *only on success*, so "ran and copied nothing", "never ran" and "ran and died" were one unchanged manifest — fifteen hours passed with nobody able to tell them apart. **`tools/sync_from_drive.py` leaves no record at all**: it prints three well-distinguished lines to stdout and writes nothing to disk, so a run that never happened and a run that copied nothing are indistinguishable the moment the console scrolls. It is *worse* than the outbound copier was, which at least stamped a manifest | `037` Part 0, from diagnosing the outbound export; `tools/sync_from_drive.py`, which writes no state; `export-run-record.md`, the outbound fix | **Whether `026`'s pair-configured copier should write the same run record.** `037` says explicitly *"do not touch `026`"* and *"if the fix genuinely must be shared between them, say so and stop"* — this row is that stop. The shape is identical and the record file would be a second value in `config/sync.yaml`, not a second mechanism. **What makes it non-obvious is direction**: the outbound copier is the only writer of its destination, so a stale mirror is its fault; the inbound one has Drive as a concurrent writer, so "nothing new" is the *expected* state most of the time and the record's value is lower. That is an argument about how loud, not about whether | PROMOTED | 2026-11-14 |
| **OBS-045** | 2026-08-14 | OBSERVATION | **The export cannot be run from a git worktree, and doing so would have produced a manifest that was complete, well-formed and wrong.** `037`'s addressing section says *"work in a worktree, not `D:\Dev\momentum`"*. Both export sources hold **untracked** files at any given moment — on 2026-08-14 that was `christoph/done/015 for christoph attach qqq.md` and four `handoff/inbox/` task files including `037` itself — and a worktree checks out tracked content only. An export run from the worktree would have copied 118 files, skipped those five, and written a manifest asserting `files 118` with a sha256 for each. **Nothing downstream could have detected the omission**: the manifest is the completeness claim, and it would have been internally consistent | `037` Part 1, from noticing that `git status` in `D:\Dev\momentum` listed five untracked files inside the two export sources; `export-handoff.ps1`, which enumerates the filesystem rather than `git ls-files` | **Whether the export should refuse to run outside the main checkout.** One line — compare `$PSScriptRoot` against `git rev-parse --path-format=absolute --show-toplevel`, or detect a `.git` *file* rather than directory — turns a silent wrong answer into a refusal. **Not added in `037`**, which was scoped to the run record and would have been adding an unrequested refusal to a script under repair. The counter-argument is real: a worktree export into a *temp* destination is exactly how `037`'s own tests exercise the copier, so any such guard must key on the destination, not on the checkout | OPEN | 2026-11-14 |
| **OBS-046** | 2026-08-14 | OBSERVATION | **OBS-034 said the worktree breakage "is also transient: removing the worktree clears it". Measured three days later, it is not — two worktrees from 2026-08-13 were never removed and `test_every_directory_holding_tests_is_declared` has been red in the main checkout continuously since.** `.claude/worktrees/024-subagent-roster` and `.claude/worktrees/029-entry-point` are both still on disk; the failure names six directories under them. **Transient is a property of the mechanism; permanent is what the habit made of it** — and a test that has been red for a known, uninteresting reason for three days is a test nobody reads any more, which is the same currency `037` was raised over | `037`, from a full-suite baseline in `D:\Dev\momentum` before any change: `8 failed, 340 passed`, one of them this; `git worktree list`; OBS-034, which predicted otherwise | **Not the exemption list — both tests forbid that in their own text, and OBS-034 already ruled it out.** What is newly settled is that *cleanup by convention does not happen*, so the fix has to be structural: either derive the scan root from `git ls-files` (OBS-034's own suggestion, unbuilt), or make worktree removal something other than a thing a session must remember. **`037` removed its own worktree and deliberately did not remove the other two** — they are another session's, and deleting another party's working checkout to make a test green is precisely the move OBS-036 was written about | PROMOTED | 2026-11-14 |
| **OBS-047** | 2026-08-14 | **READING** | **`037`'s three factual premises were each checkable and each wrong, and every one of them was inferred from the Drive mirror — the only surface the design session can see.** It states the export *"last ran 2026-08-13 20:12"* (the manifests say `2026-08-13T22:12:59+02:00`), that `christoph/done/` on Drive *"holds nothing past `c014`"* (it held `c013` and `c014`; `c013` is byte-identical to the tree, sha `3b3f5fe…`), and that `c013` is unreadable to the design session — which is the stated justification for `unblocks: 038`. **The bug was real and the fix stands.** What was actually missing from Drive was `c015` and four inbox files. **The reading**: a mirror can only be diagnosed from something that is not the mirror, and the design session has nothing else | `037`'s own text against the two `MANIFEST-*.md` files and a `sha256sum` of `c013` in both locations, run at the start of `037` | **Nothing in the repository can settle this** — it is a property of the channel, not of the code. The narrow, actionable half: **`export-run-record.md` is not exported and by design never will be**, so the design session still cannot see the instrument that answers *did the export run*. If that answer needs to reach it, the honest route is a line in the manifest quoting the record, which puts a copy of the record inside the destination it exists to be independent of — **so it is a genuine trade and not an oversight.** `037` did not take it | OPEN | 2026-11-14 |
| **OBS-013** | 2026-08-12 | ~~READING~~ → OBSERVATION | **At 209 columns each top-row tile gets ~67, below the `BOX_WIDTH` of 71 the snapshots were taken at** | Design session 2026-08-12, **as a reading**. **Measured and confirmed by `S009a` the same day** | **SETTLED.** See resolution | PROMOTED | 2026-11-12 |
| **OBS-073** | 2026-08-13 | OBSERVATION | **A `keepUpToDate` subscription dies silently and permanently, and every health signal stays green.** At 15:22:19 the `ushmds` historical-data farm broke for **2.9 s**; TWS answered all five streams with error **10182 "Failed to request live updates (disconnected)"**; the farm recovered at 15:22:22.801 and **no stream ever resumed**. **42 min 45 s** of the session went unrecorded while `ib.isConnected()` returned `True` throughout and the probe's own summary recorded `survived_window: true`. **Eight earlier farm break/restore pairs the same afternoon killed nothing** — only `ushmds`, the farm a historical request rides on | `handoff/done/021-for-code-keepuptodate-at-scale.md`, from `records/probes/021/021_run.jsonl` (35 error records) and the per-stream silent tail of 2,565.3 s | **A watchdog, and it is proposed but NOT BUILT.** `keepuptodate_stream_watchdog_s: 180` is written into 021's config proposals and lands with slice 008's loader. **The watchdog cannot key on error codes** — eight harmless breaks carried the same codes — **only on time since last update, per stream.** Until it exists, `keep_up_to_date` must not be the default for a live console: session VWAP is a stop level and a frozen one is indistinguishable from a calm market | OPEN | 2026-11-13 |
| **OBS-074** | 2026-08-13 | OBSERVATION | **The update beat is a fixed ~5 s grid — 12 ticks per minute — and cadence does NOT track print rate in the regular session.** Per-minute volume swung **23×–79×** (NVDA 29,050 → 2,176,697 shares) while updates per minute stayed at a **median of 12 for all five symbols**; rank correlation 0.10–0.29, weak **because there is no variance left to correlate**. Gaps land on multiples of 5 s with `1×` dominating (NVDA 4,354 of 4,385). A quiet stream **skips ticks** rather than slowing: pre-market AMZN medianed 14.88 s (3 ticks), SOFI 9.86 s (2), NVDA 5.00 s (1) | `handoff/done/021-for-code-keepuptodate-at-scale.md`. **Settles the question `008b` left open** — it saw a 6× swing and called the constant beat *suggestive, not established*; at 79× it is established | **Settled for TWS 178 and open for everything else.** This is a property of one broker at one server version, measured on one session. **Re-measure under another broker or after a TWS upgrade** — an append-only or event-driven feed would invert the reasoning that depends on it, and the figure is already written into a config `note:` as a source string | OPEN | 2026-11-13 |
| **OBS-075** | 2026-08-13 | OBSERVATION | **A probe reported `survived_window: true` for a run in which all five streams had been dead for 42.8 minutes.** The field is literally correct — the socket did survive — and worthless as evidence the mechanism worked. **`008b`'s done-note contains an explicit warning about this exact class of mistake in its own summary line, and that note was read before this probe was written.** The question encoded was *did the socket drop*; the question that mattered was *did data keep arriving* | `handoff/done/021-for-code-keepuptodate-at-scale.md`, §"A defect in my own instrumentation". The 021 summary JSON is **deliberately not rewritten** — it is the record of what the probe reported | **Fixed for this tool, unfixed as a pattern.** `tools/analyse_keepuptodate_scale.py` now computes a per-stream silent tail and `test_a_connected_socket_is_not_a_delivering_stream` pins it. **What is open is that nothing stops the next probe encoding the same substitution** — reading a predecessor's warning did not prevent it here, which is evidence that prose warnings do not transfer. **A liveness assertion belongs in whatever shared helper `live/` grows for broker sessions**, and no such helper exists | OPEN | 2026-11-13 |
| **OBS-047** | 2026-08-15 | OBSERVATION | **`useRTH` changes an IBKR DAILY bar, and `SPEC.md` asserted for months that it could not.** §6b.1b-ATR read *"excluded, unchangeably — ETH cannot alter a daily bar"*, and §4.4 justified the same claim from TradingView's *"Extended Hours (Intraday only)"* toggle and TC2000's equivalent. **Both are facts about charting platforms, not about IBKR's historical endpoint.** Christoph's `013` UAT measured `ATR14 13.14` against TWS's own daily ATR(14) of ≈`15.6` — **−16 %** — and `PDL 722.92` against an extended-hours low of `717.37`, a `5.55` gap. The IBKR chart tooltip states its own basis outright: `8/12 (04:00:00-20:00:00) EST` | `handoff/done/038-sessions-levels-units-windows.md`; `christoph/done/015 for christoph attach qqq.md`; `handoff/inbox/038-for-code-spec-sessions-levels-units-windows.md` Part 0 | **PROMOTED — nothing further is open.** `SPEC.md:999` and the §4.4 ADR row are corrected in place, with the struck text left visible so a reader who remembers the old rule can see that it changed. **The mechanism was never tested; §6b.1b even asked for the test** — *"pull the same daily series both ways on a symbol with a large pre-market gap and diff high/low/volume"* — and the UAT performed it accidentally, from the other end | PROMOTED | 2026-11-15 |
| **OBS-048** | 2026-08-15 | READING | **The RVOL row can name more sessions than its median was actually taken over.** `rvol_curve` keys cumulative volume by `HH:MM` and appends **only for sessions that have a bar at that minute**, so on a lookback containing a half day the median at 15:30 is over 19 sessions, not 20. `rvol_at` renders `vs {sessions_used}d median`, and `sessions_used` is the constant `RVOL_SESSIONS = 20` — **not the length of the list the median was computed from.** The number is not wrong; **its label is, at exactly the minutes where the reference thins out** | `handoff/done/038-sessions-levels-units-windows.md`, from reading `core/indicators/context.py:rvol_curve`/`rvol_at`. **A reading, not a measurement** — no half day sits in a fixture and the overstatement has not been observed on real bars | Render `len(curve_samples_at_t)` instead of the constant, so the row says `vs 19d median at 15:30h ET` when that is what it is. **Cheap and not done here**: `038` rules on session bases and units, and changing what a sample string counts is a separate decision about what the row means | OPEN | 2026-11-15 |
| **OBS-049** | 2026-08-15 | OBSERVATION | **A seventh session window is coming and the level taxonomy has six.** The SEC approved Nasdaq's 23/5 proposal on 2026-04-10 targeting December 2026; NSCC plans 24×5 clearing from 2026-06-28. `038` Part 1 fixes PDC, PDO, PDH/PDL, PMH/PML, AMH/AML and ORH/ORL against ET clock windows — **every one of which assumes a session that closes** | `handoff/inbox/038-for-code-spec-sessions-levels-units-windows.md` Part 1, recorded in `SPEC.md` §4.4a.1 | **Nothing, until it happens. `038` says explicitly: structure the taxonomy so a seventh window can be added, and BUILD NOTHING.** What would settle it is the first session that runs 23 hours: at that point `AMH`/`AML` and `PMH`/`PML` stop being separable by clock time and the definitions need re-cutting, not extending. **Do not pre-build for this** — a taxonomy fitted to a rule that has not taken effect is a guess with a version number | OPEN | 2026-11-15 |
| **OBS-050** | 2026-08-15 | OBSERVATION | **A window expressed in bars has no defined length until its bar size AND its session are both named.** A 20-period MA on hourly candles spans **three sessions** on RTH bars and **1.25** on ETH — same label, same number, **2.5× the lookback**, and no chart would ever disagree with another because both make the substitution silently. **Nothing currently rendered is exposed**: every existing value is either a daily-bar count, where one bar is one session regardless of the flag, or clock-anchored | `handoff/inbox/038-for-code-spec-sessions-levels-units-windows.md` Part 5, recorded in `SPEC.md` §4.4a.6 | **Recorded, and `038` forbids building machinery for it.** It becomes live the moment something intraday and bar-counted is first built — at which point the rule already exists to be applied rather than rediscovered. **The prohibition is the point of the row**: the obvious helpful action is to add a bar-window type nobody needs yet | OPEN | 2026-11-15 |
| **OBS-051** | 2026-08-15 | OBSERVATION | **Two session bases are declared but unruled, and they are RTH by inheritance rather than by decision.** **Half closed by `041` on 2026-08-15**: `52wH`/`52wL` are ruled RTH along with eleven other levels. **The SMA stack is deliberately still unruled** and the row stays open in substance for it — see the resolution. `038` Part 1 rules on ADR, ATR and the six named levels. **The 10/20/50 SMA stack and 52wH/52wL are none of those.** `036` proposed ETH for the SMA stack and was superseded before that row was adopted, so the stack keeps the `useRTH=True` it has had since S010. **`SMA_BASIS` and `YEAR_BASIS` exist and carry `why: UNRULED by 038`** so the gap is visible in code rather than only in a done-note | `handoff/done/038-sessions-levels-units-windows.md`; `core/indicators/context.py` | **A ruling from Christoph, and it is a real question rather than a formality.** The SMA stack is compared against price in ADR units (`ext 10/20/50`), and ADR is now explicitly RTH — so an ETH SMA would put the two sides of that ratio on different bases, which is the `OBS-037` shape. **That argues for leaving it RTH, but it is an argument, not a decision, and `038` did not make one** | PROMOTED | 2026-11-15 |
| **OBS-052** | 2026-08-15 | OBSERVATION | **Two different task files were both numbered `035`, and a third document superseded both before either ran.** `035-for-code-every-value-declares-its-session-basis.md` (2026-08-13 20:13) and `035-for-code-bug-pdl-and-atr14.md` (2026-08-14 11:55) are unrelated tasks sharing an id, and they **rule in opposite directions on `PDL` and `ATR14`** — the first ETH, the second RTH. `036` superseded the first pair; `038` superseded all three. **`tools/sync_from_drive.py` detected the collision and refused the second file, exiting 1** — the check `027` added for exactly this, and its comment already said *"numbers have collided three times here"* | `tools/sync_from_drive.py` run output, 2026-08-14; `handoff/inbox/` | **The guard worked and the numbering did not.** What is open is upstream: the design session assigns numbers by reading the inbox at a moment, and Drive introduces a gap between reading and landing. **The sync tool cannot fix this** — it can only refuse, and refusing means a task nobody sees until someone looks at the exit code. A number issued by the side that also holds the inbox would settle it | OPEN | 2026-11-15 |
| **OBS-053** | 2026-08-15 | OBSERVATION | **`52wH`, `52wL` and `ATH` will disagree with Christoph's own TradingView chart, and the disagreement is correct.** `041` rules all three RTH; **he trades with extended hours enabled**, so on any name whose 52-week extreme printed outside 09:30-16:00 the terminal will read lower (highs) or higher (lows) than the chart beside it. On QQQ they agree today. On a gap-and-fade small cap they will not | `handoff/inbox/041-for-code-spec-thirteen-levels-are-rth.md` §"The cost"; recorded in `SPEC.md` §4.4a.1 | **Nothing. This row exists to STOP an action, not to prompt one.** `041` states it plainly: *a known and accepted divergence, not a defect — do not "fix" it later.* **The default action on finding this is wrong**, which is why it is written down: a future session sees the terminal disagreeing with the chart, reads it as the `ATR14` defect recurring, and flips a flag that breaks the composition chain `041` exists to protect. **`c021` is the UAT, and a disagreement there is the ruling working** | OPEN | 2026-11-15 |
| **OBS-054** | 2026-08-15 | OBSERVATION | **A threshold comparison at an exact edge misfiled a trade into the one class that feeds a limit, and binary floating point was the whole cause.** `039` Part 2 closes the break-even band at its edges — `-0.05R <= R <= +0.05R`. A $10.00 fill exiting $9.95 against a $9.00 stop is exactly `-0.05R`; the subtraction yields `-0.050000000000000710`, which is **outside** the band, so a scratch that made nothing and lost nothing classified as `L` — **the only class that feeds `losses_max_day`, `losses_max_month` and both R-lost caps. A limit firing on a rounding error.** Fixed in `core/risk/classify.py` with a `1e-9` edge tolerance that resolves an on-the-edge value to the more conservative class | `039`, from `core/tests/test_trade_classification.py::test_each_class_at_and_around_its_boundary` on its first run — the parametrisation names the exact edges, which is why it fired at all | **Whether any other threshold comparison in this tree is exposed, and nobody has looked.** The instance is fixed; the class is not. Every `>=`/`<=` against a configured threshold on a value derived by subtraction or division has the same shape — the tight-stop rule (`0.25 ATR`), the `<= 1.0 ADR` stop ceiling, the `+-0.02ADR` anchored-window band `038` specified, and the `80%` override-rate prune in §7b.4. **What would settle it: a sweep that lists every threshold comparison and states, per site, whether an exact-edge input is reachable and what it costs.** Not done here — `039` owns one module | OPEN | 2026-11-15 |
| **OBS-055** | 2026-08-15 | OBSERVATION | **`ema9` and `ema21` at entry are a stated data-collection requirement with no screen presence at all, and nothing computes them.** `c015` §5b calls them a *"new requirement for trade data collection"*; `039` Part 6 lists them among the trade-record fields and says explicitly that they are **never rendered**. **A requirement with no screen presence is one nothing will ever surface as missing** — every other field in Part 6 shows up on the TRADE panel and would be noticed as a gap; these two would not. **No trade record type exists in the tree at all**, so all six fields are absent, but the other five have a rendering consumer waiting for them | `039` Part 6, which asks for exactly this row if the fields are not built; `docs/specs/SPEC.md` §7b.1f, where the table is now recorded | **Building the trade record, which is a slice nobody has scheduled.** `039` is specification plus classification arithmetic and explicitly builds no persistence. What is narrower and actionable: **`ema9`/`ema21` must be captured at ENTRY, not reconstructed later** — an EMA at a past instant cannot be recovered from daily bars, so a record written without them cannot be repaired retrospectively. That makes this expensive-to-defer in the same way `stop_at_entry` is | OPEN | 2026-11-15 |
| **OBS-056** | 2026-08-15 | OBSERVATION | **A closed trade that cannot be classified counts toward no limit, so the day's trade cap can be exceeded by trading without a recorded entry stop.** `039` Part 2 requires `trades = W + P + L + BE` and Part 3 makes `trades_max_day` a limit on `trades`. A trade with no `stop_at_entry` renders `unavailable (no entry stop recorded)` — correctly, and `039`'s refusal test demands it never default to break even — but it therefore joins none of the four classes and is invisible to every counter. **The invariant and the cap cannot both be satisfied by the same number.** `Counters.unclassifiable` counts them and `Counters.closed_total` includes them; **nothing reads `closed_total`** | `039`, from implementing Part 2's asserted invariant against Part 3's limit table; `core/risk/classify.py`, `Counters`; `core/tests/test_trade_classification.py::test_an_unclassifiable_trade_is_counted_and_is_outside_trades` | **A ruling on which number `trades_max_day` compares against** — the classified count, or every closed round trip. Both are defensible: the cap exists to stop overtrading, which argues for `closed_total`; the invariant exists so the class breakdown reconciles, which it still does either way. **Implemented exactly as `039` asks rather than resolved by quietly redefining `trades`**, because redefining it would make the asserted invariant false and no test would say so. Note that `040` bears on how often this can happen at all | OPEN | 2026-11-15 |
| **OBS-057** | 2026-08-15 | OBSERVATION | **`config/risk.yaml` is a config file with no reader, and it declares the number every position size will come from.** `039` builds no panel and no limit enforcement, so nothing in the tree loads it — `risk_usd_per_trade: 500` is the declaration of 1R and no code path reaches it. **This is the state in which a value gets edited, has no effect, and is later read as the one in force**; `config/ibkr.yaml` avoided it only because `034` wired it the same day. `tests/test_risk_config_matches_core.py` is a stand-in reader pinning the two classification thresholds and the commission basis against `core/risk/classify.py`, **and it deliberately does not pin the seven limit values** — they have no code counterpart, so asserting them against a second copy of the same literals would be a test of nothing | `039`, from writing the file with no consumer; `config/risk.yaml`; `tests/test_risk_config_matches_core.py`, whose own docstring records the gap | **The slice that builds the limits — `S011`.** Until then the exposure is that seven of nine values in the file are unverified by anything. **The wrong fix is to delete the unread keys**: they are the record of a ruling made on 2026-08-14 and re-deriving them later from a done-note is how a decision becomes a reconstruction. **The other wrong fix is a test that reads the yaml and asserts the literals back** — green forever, and it would have to be edited in step with any change, which is precisely the drift it would claim to catch | OPEN | 2026-11-15 |
| **OBS-058** | 2026-08-15 | OBSERVATION | **Two sessions allocated `OBS-053` to different findings on the same day, in the ledger built to stop findings going missing.** `039` branched from `79e95f4` when the highest row was `OBS-052`, wrote `OBS-053`–`OBS-056`, and hit a git merge conflict because another session had meanwhile committed its own `OBS-053` about `52wH`/`52wL`/`ATH`. **Git caught it only because both rows landed on the same line of the same file** — had the other session appended a blank line first, both `OBS-053`s would have merged cleanly and the duplicate would be in the ledger now, with two findings sharing one id and every later citation ambiguous. `039`'s four renumbered to `OBS-054`–`OBS-057` and two forward references in `SPEC.md` §7b.1b/§7b.1f were corrected with them | `039`, from the merge of `worktree-039-risk` into `main`; `OBS-052` is the same failure one level up, in task-file numbering, and `031` is the general fix and is unbuilt | **Nothing checks that ledger ids are unique.** `tests/test_observations_ledger.py` validates schema, status and `review-by` and **never looks for a duplicate id** — the one property a register of record most needs. That is a five-line test and it is not written here, because writing it inside a slice about risk is how a task acquires work nobody scoped. **The wider fix is `031`.** What makes this row worth keeping separately from `OBS-052` is that the collision happened in the instrument, not in the work it tracks: **a register that can hold two of the same id cannot answer *what did we find*, which is the only question it exists for** | OPEN | 2026-11-15 |
| **OBS-059** | 2026-08-15 | OBSERVATION | **`SPEC.md` §4.1 offers a market measurement no colour at all — not one, zero — and `042` Part 4 asks for two distinguishable ones.** §4.1 binds colour to KIND: **blue** is a declared parameter, **amber** a failed rule, **green/red** a *fitted* signal measured against its pre-registration, **dim+badge** the system refusing. `042` Part 4 rules that `gapped over` (a state), `clear for 0.19ADR` (a distance) and `▲ above`/`▼ below` (a side) must not share a colour. **None of the three is a rule, a signal, or a refusal** — they are measurements about the market, and the palette has no entry for that kind. Green and red are additionally unavailable by construction, since nothing in this system is fitted. **The ruling is therefore satisfied vacuously today**: all three render in the default foreground, which does not conflate them with a verdict and equally does not distinguish them from each other | `042` Part 4, which says *"if it permits only one non-default colour, say so and stop rather than inventing a second"*; `SPEC.md` §4.1's four-colour grammar table | **A decision only Christoph can take, and `S012` will hit it.** Either the palette gains a kind for *measurement*, which is a §4.1 amendment and reopens the question §4.1 exists to have closed, or **the distinction is carried by the non-colour channel — typography, glyph or position — which §4.1 already requires anyway** (*"colour is still never the only channel"*, for colourblind accessibility). **The second needs no amendment and is almost certainly right**, but `042` forbids a slice choosing, and choosing here would set the precedent that a palette rule can be widened by whoever next needs a colour | OPEN | 2026-11-15 |
| **OBS-060** | 2026-08-15 | OBSERVATION | **One row of the units table is recorded and unenforced, and nothing on screen would show it.** `SPEC.md` §4.4a.4 requires times to render `HH:MMh` 24-hour with `ET` — *"`ET` is not optional... `034` lost four values to a UTC/ET slicing defect"*. **There is no `Unit.TIME`.** `config/formatting.yaml` declares six units — dollar, percent, adr, multiple, shares, count — and no clock. Every time currently on the panel is built as a STRING at its call site: the VWAP anchor's `04:00 anchor`, the context block's `as of 13:24 · lag 35s`. **So the one unit rule motivated by an actual incident is the one the formatter cannot enforce**, and `test_every_rendered_number_carries_a_unit` cannot see it because it iterates `Measured` values and a time is not one | `042` Part 2 and `SPEC.md` §4.4a.4; `config/formatting.yaml`, six units; `live/tui/numbers.py`, which renders `Measured` only | **Whether a clock is a `Unit` at all.** It is a defensible no — a timestamp is not a quantity and forcing it through a numeric formatter to gain a suffix would be shaping the mechanism to the test. **The alternative is a separate render path with its own test**, which is more honest and is real work. What makes this worth holding rather than dropping: **the rule exists because of a defect that already happened**, and the values it governs are exactly the ones a Cape Town reader cannot check by looking out of the window. Not built in `042`, which changes spacing and adds no unit | OPEN | 2026-11-15 |
| **OBS-061** | 2026-08-15 | OBSERVATION | **The only externally-verified fixture in this repo cannot check the 15-minute opening range, and the obvious fix would fabricate evidence.** `live/tests/test_qqq_2026_08_13_regression.py` pins `PDH`, `PMH`, `PML`, `ORH5`, `ORL5` against Christoph's own IBKR chart for 2026-08-13 — **the only outside verification this project has.** Its bar series runs 04:00 to 09:35, so under `042` Part 1 the 15-minute window never closes and `ORH15` correctly refuses. **Extending the series to 09:45 would mean inventing ten minutes of bars nobody read off a chart, inside the one file whose values came from outside this repo** — and the invented bars would then sit beside the verified ones, indistinguishable, in a file whose whole authority is that it was checked externally | `042` Part 1, from the fixture refusing `ORH15` on the first run after the rename; `live/tests/test_qqq_2026_08_13_regression.py` | **A second chart reading from Christoph, covering 09:35–09:45 on a session he still has**, which is the only thing that can extend an externally-verified fixture without weakening it. Until then the containment property is asserted in `core/tests/test_opening_range_windows.py` against synthetic bars built so the two windows genuinely disagree — **which tests the arithmetic and not the market.** The distinction matters: the synthetic test would pass against a rail that sliced the right windows out of the wrong series, which is precisely the defect `034` shipped and `038` found | OPEN | 2026-11-15 |
| **OBS-062** | 2026-08-15 | OBSERVATION | **Four ledger ids are used twice, and one of the pairs cannot be resolved without saying which finding is meant.** `OBS-044`, `OBS-045`, `OBS-046` and `OBS-047` each appear on two rows: a 2026-08-13 set from `021` (a `keepUpToDate` subscription dying silently · the ~5 s update beat · `survived_window: true` on dead streams) and a 2026-08-14 set from `037` (the inbound copier leaving no record · the export not being runnable from a worktree · worktrees outliving their tasks). **`tests/test_observations_ledger.py` does not check id uniqueness**, so nothing went red. `043` and `041` both cite the 2026-08-14 meanings and both are correct; **this session read the 2026-08-13 rows and reported in `041`'s done-note that `041`'s citations pointed at unrelated rows. That claim was wrong and is corrected here** | `handoff/done/043-third-pair-and-two-instruments.md`; `docs/observations/OBSERVATIONS.md` itself | **Christoph or the design session decides which set is renumbered, because both are cited from outside this file** — `037`, `041` and `043` name them in task files and done-notes already written and exported, and renumbering either set breaks those references. **This is the `035` collision (`OBS-052`) one level in**: the guard that catches colliding task numbers has no counterpart for ledger ids. **Do not renumber unilaterally, and do not resolve either `OBS-044` while both exist** — marking "OBS-044 PROMOTED" would read as closing *"a `keepUpToDate` subscription dies silently and every health signal stays green"*, which is a live finding about a value used as a stop level and is **not** closed. An id-uniqueness assertion in the ledger test is the cheap half and can land immediately | OPEN | 2026-11-15 |
| **OBS-063** | 2026-08-15 | OBSERVATION | **The Drive folder naming convention now points both ways and `momentum-christoph-open` reads as the wrong one.** Inbound pairs so far carry `inbox` in the name (`momentum-inbox-handoff`); outbound ones carry the repo folder they mirror (`momentum-code-handoff`, `momentum-christoph-done`). **`momentum-christoph-open` follows the outbound shape and is inbound** — it is `043`'s third pair, Drive → `christoph/open/`. Christoph named it for symmetry with `momentum-christoph-done`, which is a defensible reading: the two folders are the two halves of one exchange with him, and the direction is the less salient fact about them | `handoff/inbox/043-for-code-task-third-pair-and-two-instruments.md` §Part 1, which records the ambiguity and instructs that it not be fixed; `config/sync.yaml` | **Nothing, and `043` is explicit: record it, do not rename anything.** Renaming a Drive folder breaks the one thing that has to keep working across a boundary neither side can see. **What would settle it is a naming rule stated once** — direction-first or repo-folder-first, applied to the next folder created rather than retrofitted to five. Until then the ambiguity is on the ledger rather than in somebody's head, which is the whole point of the row | OPEN | 2026-11-15 |
| **OBS-064** | 2026-08-15 | OBSERVATION | **The test suite was overwriting the repository's inbound run record, and the new `verify.ps1` section reported it as a real sync.** Section 6 rendered `outcome : t: 1 new · 031-for-code-thing.md` — **a pytest fixture's filename, describing a copy into a temp directory, in the tracked artifact whose only job is saying when the real sync last worked.** Every test that drove `main()` with its own `--config` and no `--record` wrote the default path. **Found within minutes of the section existing, by reading its output** — the defect predates the section by however long those tests have run, and nothing could see it before | `handoff/done/043-third-pair-and-two-instruments.md`; `verify.ps1` section 6 output, 2026-08-15 | **Fixed structurally rather than by convention**: `main()` now derives the record path from the config when `--config` is not the default, so a test cannot reach the real record even by forgetting. Passing `--record` from each test would have worked until the next test was written. **What is open is the general form** — `export-handoff.ps1` takes a `-RunRecordOverride` and relies on callers using it, which is the same gap in the outbound direction, unmeasured. **This is `043`'s own subject arriving twice in one task**: an instrument that reports on everything except itself, and then a new instrument whose first output was about itself | PROMOTED | 2026-11-15 |
| **OBS-065** | 2026-08-15 | OBSERVATION | **`ask` does not bind in auto mode. `deny` does.** Three probes against the live session immediately after `58b0926`: `deny` on a path (a `.env`-shaped `Read`) **BLOCKED**; `deny` on a shell (`git clean -n -d`) **BLOCKED**; `ask` on a shell (`git push --dry-run origin main`, which matches `Bash(git push:*)` in `ask` and **not** the exact-string `allow`) **RAN UNPROMPTED**. The file was loaded — two `deny` rules fired from it in the same minute — so this is not "settings ignored". **The cause is auto mode: shell routes through the classifier, which approves what `ask` would have prompted for.** The consequence at the time was total: every route to another session's worktree or to uncommitted work sat in `ask`, **the one class that did not bind**, so a policy that read as complete protected none of it | `handoff/done/046-permission-policy.md` §1; commits `58b0926`, `f35bd3a` | **Whether `ask` binds OUTSIDE auto mode, which is NOT measured and must not be assumed either way.** All three probes ran in one session in one mode, so the claim is *`ask` does not bind here*, never *`ask` never binds*. A session in default permission mode running the same three probes settles it in minutes. **The durable half needs no further work and is the part to carry: a probe every hypothesis predicts is not a measurement.** Plain `git push origin main` succeeding proves nothing — pushes were unprompted before the policy file existed — and `--dry-run` was chosen precisely because the three hypotheses give three different answers | OPEN | 2026-11-15 |
| **OBS-066** | 2026-08-15 | OBSERVATION | **Permission rules are PREFIX matches, and two reachable spellings step around the irreversible-command denies.** `git reset HEAD~1 --hard` **reorders the flag past the prefix**, so `Bash(git reset --hard:*)` never matches; `git checkout <file>` **discards without the `--`**, so `Bash(git checkout --:*)` never matches. Both destroy uncommitted work. **Neither is covered by any rule and neither can be** — a prefix reads a string, and these are the same command wearing a different string | `handoff/done/046-permission-policy.md` §3; `.claude/settings.json`; `f35bd3a`, which records the gaps rather than papering over them | **Nothing, in the sense of a fix — the gaps are structural to prefix matching. What is open is a MISREADING, and that is why the row exists.** What covers them today is `autoMode.classifyAllShell: true`, which classifies every shell command instead of matching a string. **So the rule list is not the whole protection, and outside auto mode these two spellings are covered by nothing at all.** A reader auditing `.claude/settings.json` will count the denies and conclude the class is closed; it is closed only while auto mode is on. **The wrong action on finding this is to add more prefix variants** — `--hard` after a ref, before a ref, with `=`, with a path — which is a list that can never be complete and would read as if it were | OPEN | 2026-11-15 |
| **OBS-067** | 2026-08-15 | OBSERVATION | **A `Bash`-only permission policy is one environment variable away from no policy, and the file reads identically either way.** Setting `CLAUDE_CODE_USE_POWERSHELL_TOOL=1` switches Claude Code to the PowerShell tool. All **eleven** `deny` rules committed in `58b0926` and `f35bd3a` were spelled `Bash(git ...)` and there were **zero** PowerShell denies, so `PowerShell(git worktree remove ...)` matched nothing and every protection verified two commits earlier **stopped applying — not loosened, ABSENT.** Found by being handed the env var, not by reading the policy. **PowerShell aliases were a second live hole**: `rm`, `ri`, `del`, `erase`, `rd`, `rmdir` all resolve to `Remove-Item` and `mv`/`move`/`mi` to `Move-Item`, and only the canonical names were in `ask` | `handoff/done/046-permission-policy.md` §4; commit `e2e3a93`; `tests/test_permission_policy_is_shell_symmetric.py`, seen RED against the exact state of `f35bd3a` — 8 `ask` and 11 `deny` rules reported Bash-only | **The structural half is closed and the behavioural half is NOT, and the row is open for the second.** `tests/test_permission_policy_is_shell_symmetric.py` asserts every git rule in `ask` and `deny` has a twin under both tools, naming the eight worktree/uncommitted-work denies explicitly so it cannot pass vacuously against a policy with no git rules (`OBS-037`'s shape). **That proves the entries EXIST. It does not prove the PowerShell tool honours them** — which needs a session with the env var set and `OBS-065`'s three probes re-run, and is exactly the measurement `f35bd3a` performed for Bash. **What makes this worth a row rather than a fix note: the artefact under review was correct both before and after.** A reviewer reading the policy at `f35bd3a` would confirm it was right, and it was right for one of the two shells it had to hold under | OPEN | 2026-11-15 |
| **OBS-068** | 2026-08-15 | OBSERVATION | **`verify.ps1` does not parse under Windows PowerShell 5.1 and dies before running a single test.** Measured both ways with `Parser::ParseFile`: **5.1 → 10 parse errors** (*"Unexpected token 'files' in expression or statement"*), **PowerShell 7.6.4 → 0**. **The cause is encoding, not syntax:** the file is UTF-8 **without a BOM** and holds **78 non-ASCII bytes** — em-dashes and `·` in its section headers — and 5.1 reads a BOM-less file as ANSI, so a mojibake `"` lands inside a string literal and the quoting unbalances for the rest of the file. **Only `verify.ps1` is exposed**: `sync.ps1` and `export-handoff.ps1` contain zero non-ASCII bytes and run identically under both. **`045` documented `verify.ps1` as a one-word command runnable from a cold terminal, and whether that terminal is `pwsh` is stated nowhere** — `powershell` is still what a default Windows shortcut means | `handoff/done/046-permission-policy.md` §11, from `046`'s own closing verification failing on the first attempt; `[Parser]::ParseFile` under both editions | **A choice among three fixes, and it belongs to `045`'s area rather than `046`'s** — add a BOM, replace the 78 characters with ASCII, or document `pwsh` as required. **The third is the weakest**: it puts the burden on the reader `045` was written to protect. **The one thing in this defect's favour is that it is loud** — the script refuses to run rather than running and reporting wrongly — so nobody has ever acted on a wrong `verify.ps1` output because of it. **What is genuinely unknown is whether Christoph's own terminal is `pwsh`**, which is a one-line question and settles whether this has ever affected him or only a session that reached for the wrong interpreter | OPEN | 2026-11-15 |
| **OBS-069** | 2026-08-15 | OBSERVATION | **A deny rule cannot be scoped to a filesystem path for a shell deletion, and the rule that tries sits in the policy looking exactly like one that works.** Two probes, same `deny` array, same well-formed file, same session: **`Bash(rm -f D:/Dev/momentum/.probe-deny/a.txt)` — an exact command string — BLOCKED**, file survived; **`Bash(rm //d/Dev/momentum/.probe-deny/**)` — a path glob — RAN, file deleted.** A `Bash(...)`/`PowerShell(...)` specifier is a **command-string prefix**; only `Read`, `Edit` and `Write` take path patterns, so a path inside a shell rule is literal command text. **Three further defeats even if the syntax existed:** a relative path, a `cd` first, and **`allow` containing a general-purpose interpreter** — `Bash(C:/venvs/trading/Scripts/python.exe:*)` — which can delete anything and which no command-string rule can see into. **Note the existing `Write`/`Edit` denies on `records/`, `christoph/` and `.claude/worktrees/` protect against WRITING, not deleting**: there is no delete tool, so deletion has exactly one route and it is the unscopeable one | `handoff/inbox/048-for-code-task-the-ask-audit.md`, from the two probes run 2026-08-15 while auditing `ask`; `.claude/settings.json` | **The fallback is `ask`, already in place, and the real fix is upstream: stop creating the paths.** Most `Remove-Item` prompts today are for temp directories created **inside** `D:\Dev\momentum` — the prompt protects nothing and trains the habit of approving deletions in the repo. `048` now carries the rule: **tools and probes write scratch to `$env:TEMP`, never into the tree.** What is open is the sweep — which tools and tests currently create scratch inside the repository, which nobody has counted. **Two expressible alternatives exist and both are decisions, not edits:** a `PreToolUse` hook, which sees the command and can resolve paths (precedent `spend_guard.py`, and **`~/.claude/hooks/**` is denied by this very policy, deliberately**), or `sandbox.filesystem.denyWrite`, which is genuinely path-based and merges with `Edit(...)` denies but governs sandboxed commands only and needs the sandbox enabled | OPEN | 2026-11-15 |
| **OBS-076** | 2026-08-16 | OBSERVATION | **054 Part 5b instructed editing `handoff/inbox/006-ranked-watchlist-panel.md` and `007-watchlist-ingestion-amendments.md` to add a staleness banner about a first-generation mockup citation, and both files are EVIDENCE-CARRIED, not adopted.** `EVIDENCE-CARRY.md` rows 181-182 hash-verify them byte-identical against `momentum-harness`, and `tests/test_evidence_carry_intact.py` went red immediately on the edit. The task instruction and the evidence-carry invariant point in opposite directions -- one says annotate the live document in place, the other says never clean, dedupe, reformat, prune or regenerate evidence, ever. **The edit was reverted rather than kept**, and this row carries the finding instead: `006` and `007` cite a first-generation mockup and are stale in more than their filename, per `052` v4.0. | `054` Part 5b, and independently, `EVIDENCE-CARRY.md` rows 181-182 · `tests/test_evidence_carry_intact.py` red on the attempted edit, quoted in `handoff/done/054-unblock-the-queue.md` | **A task file that annotates evidence needs a different mechanism than editing the evidence itself** -- a companion note beside it, a line in the task queue, or a decision that these two tasks are refused outright rather than amended. That decision is Christoph's, and until it is made `006` and `007` should not be run on their current visual contract | OPEN | 2026-09-16 |
| **OBS-070** | 2026-08-16 | OBSERVATION | **`SPEC.md` cites six mockups by number and three of those numbers name nothing that exists anywhere.** `docs/specs/mockups/` holds exactly five sheets — `mockup-01` through `mockup-05` — plus `mockup-README.md`. `SPEC.md` cites `mockup-06` (§ tape, line 1611), `mockup-07` (§ sizing, lines 1931 and 2001) and `mockup-08` (the hotkey table, line 194) as though they were sheets in that folder. **They are not in the tree and they are not in Drive under those numbers either**: `052`'s rename table maps `mockup-08` to `TAPE mockup — twelve tape states`, a second-generation sheet about a different subject, so the number collides across two generations and resolves to the wrong artifact in the one place it still resolves at all. **The whole citation set is first-generation numbering**, which `052` retired on 2026-08-16 | `handoff/inbox/052-for-code-task-product-spec-pointer.md` Part 3, from the tree-wide `mockup-` sweep run under `052`; `docs/specs/SPEC.md` lines 124–128, 191–194, 1611, 1931, 2001, 2646; directory listing of `docs/specs/mockups/` | **Not a rename — `052` says explicitly that a first-generation citation is a finding and must not be patched.** What would settle it is the reconciliation `052` scopes out in its own words: `SPEC.md` against the product spec set, with `MOCKUP-INDEX` naming which sheet each section now means. **Until then the cheapest correct action is nothing** — rewriting `mockup-08` to the second-generation name would point the hotkey table at a tape mockup and be indistinguishable from a repair | OPEN | 2026-11-16 |
| **OBS-071** | 2026-08-16 | OBSERVATION | **Two inbox files with no done-note are live instructions built on retired mockups.** `handoff/inbox/006-ranked-watchlist-panel.md` and `handoff/inbox/007-watchlist-ingestion-amendments.md` have no counterpart in `handoff/done/` — every neighbour from `004` to `008b` does — so by the addressability rule they are still open work. Both instruct against first-generation sheets: `006` tells the builder to read `mockup-README.md` first and cites `mockup-03` five times; `007` orders a re-draw of `mockup-01-ingest.html`. **`052` states the reading plainly: a live instruction citing a first-generation mockup is probably stale in more than its filename** — those sheets predate Textual, the TRADE consolidation and three deleted features, and the inbox has since reached `052` | `handoff/inbox/052-for-code-task-product-spec-pointer.md` Part 3, from the `mockup-` sweep; directory listing of `handoff/done/` showing no `006-*` or `007-*` | **One question to Christoph, not a repair: are `006` and `007` still owed, or were they overtaken?** If overtaken they need a done-note or a supersession recorded, because nothing in the tree distinguishes an abandoned task from a pending one — `NOW.md` will keep reporting them as ready. **Do not re-point their mockup citations**: a renamed citation inside a stale instruction makes it look current | OPEN | 2026-11-16 |
| **OBS-072** | 2026-08-16 | READING | **`052`'s rule that nothing is referenced by document link has one file where applying it destroys the file's function, and that file is inside the folder the rule names.** `docs/specs/DRIVE-ARCHIVE-LIST.md` carries **40 `docs.google.com/document` and `drive.google.com/file` links** and its own header reads *"Read as instruction, not record"*, so it falls squarely inside `052` Part 2's *live instructions: `docs/specs/*`*. **But its links are archive TARGETS, not references to authority** — the rule's stated rationale, that a link dies on revision and may resolve to a superseded document, is the property this file is exploiting: it exists to point at superseded documents so they can be moved. **And name-plus-folder cannot express what it says**: two rows name the same document, `Failed-Bounce Rollover Short Playbook`, at 2,812 and 6,460 bytes, and the file id is the only thing distinguishing them | `handoff/inbox/052-for-code-task-product-spec-pointer.md` Part 2, from the tree-wide link sweep run under `052`; `docs/specs/DRIVE-ARCHIVE-LIST.md` lines 20–39, 49–54, 74–86 | **Left unrewritten under `052` and recorded here rather than decided.** What would settle it is a one-line ruling from Christoph or the design session: either the no-document-link rule carves out archive manifests, or `DRIVE-ARCHIVE-LIST.md` is reclassified as a record — its 32 recommended moves were never made and `H7` still owns them, so **whether it is instruction or history is genuinely undecided**, and that ambiguity is the real finding | OPEN | 2026-11-16 |
| **OBS-077** | 2026-08-22 | OBSERVATION | **Retiring a `christoph/open/` file locally does not stop it reappearing — the `christoph_open` sync pair has no counterpart for the copy-verify-retire that removes it from Drive.** `056`'s own `sync.ps1` run re-copied `018-for-christoph-task-check-atr14-and-pdl.md` into `christoph/open/` as `1 new`, byte-for-byte the unfilled template, in the same run that also carried `056` itself in — even though Christoph had already retired `018` (moved his filled-in copy to `christoph/done/018-*`, committed one commit earlier in this same session). **Diffed directly: the reappeared file is the pre-fill template**, missing the two verified-fixed lines and both measured level values Christoph's `christoph/done/018-*.md` carries | `056`'s own `sync.ps1` run, 2026-08-22, comparing the reappeared `christoph/open/018-*.md` against the just-committed `christoph/done/018-*.md`. **Confirmed a second time in the same session**: Christoph retired `021-for-christoph-task-52-week-basis.md` mid-session while `056` was on its branch, and the closing sequence's own `sync.ps1` re-run reappeared its pre-fill template in `christoph/open/` the same way, within the hour — not a one-off. **Confirmed a THIRD time, at scale, the same session**: Christoph retired nine more decision files in one pass (`023`–`029`, `031`, `032`) and the very next `sync.ps1` run reappeared all eleven templates (`018`, `021`, `023`–`029`, `031`, `032`) in a single `11 new` line — the count scales exactly with how many get retired at once, so this is not a rare edge case, it is what happens on **every** retirement, **every** time, without exception observed so far | **Whether the `christoph_open` pair needs a delete-on-retire counterpart, or whether retiring in Drive itself is Christoph's own step that simply has not been taken yet.** `config/sync.yaml`'s own comment for this pair already rules that nothing is ever written to, deleted from or renamed in a `from` folder — so the sync tool cannot be the one to remove the Drive original even if this is a genuine gap. **Not `056`'s to fix** — it only fixes a false-green guard and a dependency parser | OPEN | 2026-11-22 |

---

## The UAT review register — 018 part 5

**The gap this closes.** This ledger has a trigger that goes red. **A signed UAT sitting in
`christoph/done/` has none.** A finding written into a retired UAT reaches work only because
the design session happened to be in the conversation — **three of `018`'s parts exist for
exactly that reason, and nothing would have caught their absence.**

**The shape chosen: the register keys on the LEDGER, not on the UAT.** Every file in
`christoph/done/` must appear here with a status. `tests/test_observations_ledger.py` goes red
on a retired UAT with no row at all, and `NOT REVIEWED` rows carry a `review-by` and go red
when overdue, exactly like an observation.

**Why not the structural shape** — a `**Findings**` section authored into each UAT. `018`'s own
*Do not* list forbids writing to `christoph/`, and so does `CLAUDE.md`. **A check requiring a
section this session cannot add, to thirteen files it cannot edit, would be red on arrival with
no legal route to green.** It is the better shape and it is not available to this side of the
channel — if the design session authors `**Findings**` sections into future UATs, this register
becomes the weaker of two mechanisms and should give way to it.

**Its limit, stated rather than discovered.** **It cannot detect a finding the reviewer
overlooked.** It forces someone to look and to record that they looked; it does not verify the
quality of the look, and a reviewer who marks everything `NO FINDINGS` passes it. What it does
catch — and what actually happened with `009` — is a UAT retired with findings that were never
routed anywhere, which now goes red instead of staying silent.

| uat | status | destination / note | review-by |
|---|---|---|---|
| `009-s009a-read-the-screen-at-working-width.md` | **CITED** | OBS-014, OBS-015, OBS-016 | — |
| `011-018-shrink-it-grow-it-back.md` | NOT REVIEWED | **The register's first live catch.** Retired by Christoph after this mechanism existed, and the test went red naming it — which is exactly what `018` part 5 was built to do | 2026-09-12 |
| `001-ibkr-totalview-api-entitlement.md` | NOT REVIEWED | — | 2026-09-12 |
| `002-handoff-protocol-rule-4-uat.md` | NOT REVIEWED | — | 2026-09-12 |
| `003-s009-read-the-empty-screen.md` | NOT REVIEWED | — | 2026-09-12 |
| `004-m001-count-the-tree.md` | NOT REVIEWED | — | 2026-09-12 |
| `005-012a-depth-book-comparison.md` | NOT REVIEWED | — | 2026-09-12 |
| `006-h8-snapshot-path-fills.md` | NOT REVIEWED | — | 2026-09-12 |
| `007-h10-regime-prompt-v12.md` | NOT REVIEWED | — | 2026-09-12 |
| `008-h11-supersession-review.md` | NOT REVIEWED | — | 2026-09-12 |
| `010-016-read-verify-cold.md` | NOT REVIEWED | — | 2026-09-12 |
| `012-uat-first-five-minutes.md` | NOT REVIEWED | — | 2026-09-12 |
| `012-uat-first-five-minutes_1.md` | NO FINDINGS | **REMOVED by Christoph, 2026-08-12.** It was not a duplicate — different sha256, 821 bytes smaller — and `018` reported two versions of one signed pre-registration with nothing declaring which was authoritative. **He resolved it, which only he could.** The row stays so the ambiguity's existence is not lost with the file | — |
| `012b-uat-basis-correction.md` | NOT REVIEWED | — | 2026-09-12 |

**Why twelve rows say `NOT REVIEWED` rather than `NO FINDINGS`.** Judging whether a retired UAT
contains an actionable finding is a reading, and it is the design session's reading to make.
**Asserting *no findings* on twelve files this session did not author, did not run, and cannot
ask about would be exactly the vacuous pass `018` warns against** — a green check that
establishes nothing. They are recorded as a declared backlog with a date, which is the
instrument this ledger already uses for everything it cannot settle yet.

**`review-by` is 2026-09-12, one month** — shorter than the observations' three, because this
is a reading task blocked on nothing. It can be done in an afternoon by whoever is next in the
conversation.

---

## Resolutions

> **Resolved for three of the four ids, 2026-08-16 under `053` Part 1.** `OBS-044`, `OBS-045`
> and `OBS-046` each named two findings — one dated 2026-08-13 from `021`, one dated
> 2026-08-14 from `037`. **`037`'s rows keep the numbers; `021`'s three rows moved forward to
> `OBS-073`, `OBS-074` and `OBS-075`**, ids read from this ledger at execution time.
> **The resolutions below belong to the 2026-08-14 rows and each says so in its first line.**
>
> **`044`'s rule was *the earlier allocation keeps the number*, and `021`'s rows were the
> earlier ones.** It was applied in reverse deliberately: the rule was only ever a means to the
> end **do not change what an exported done-note appears to have said**, and it was written
> under a false belief about which set came first. `021`'s done-note cites none of the three, so
> moving them breaks nothing; the letter would have retargeted twenty citations across nine
> exported files. **When a means and its end separate, the end is the durable half.**
>
> **`OBS-047` is a PERMANENT COLLISION and was NOT resolved.** See the record directly below.
> The collision is also recorded as `OBS-062`, and id reuse in general as `OBS-063`.

### `OBS-047` — a permanent collision. Do not reallocate either row.

**Two findings carry this id and both keep it.** This is the one pair that cannot be resolved
by moving a row forward, because **each side is already cited by an exported done-note**, and
the whole purpose of the reallocation rule is *do not change what an exported done-note appears
to have said*. Moving either row would break that end in order to satisfy its means.

| Row | Kind | Finding | Cited by (exported) |
|---|---|---|---|
| **`OBS-047` · 2026-08-14** | **READING** | `037`'s three factual premises were each checkable and each wrong, and each was inferred from the Drive mirror | `handoff/done/037-drive-export-stopped.md` lines **120**, **391**, **422** |
| **`OBS-047` · 2026-08-15** | OBSERVATION | `useRTH` changes an IBKR **daily** bar, and `SPEC.md` asserted for months that it could not | `handoff/done/038-sessions-levels-units-windows.md:294`; `handoff/done/041-thirteen-levels-are-rth.md:144` |

**Each row names the other.** A reader arriving at either from a citation must be able to tell,
without guessing, that the id is ambiguous and which of the two the citing note meant.
**Disambiguate by DATE, always** — `OBS-047 (2026-08-14)` or `OBS-047 (2026-08-15)`. An undated
reference to `OBS-047` is defective and should be reported rather than interpreted.

**Why this is not unfinished work.** `053` Part 1 ruled on three ids. **The ledger holds four,
and `OBS-062` had already said so** — *"one of the pairs cannot be resolved without saying which
finding is meant."* The ruling was made from the question file rather than from the ledger, so
the fourth was never in scope. **This record is the resolution**: the collision is permanent,
documented and disambiguated by date, rather than left open.

**OBS-044 (the 2026-08-14 row — *the inbound copier leaves no record*) · PROMOTED.**
`resolution:` Became `043` Part 2. **`tools/sync_from_drive.py` now writes
`sync-run-record.md` at the repository root on every invocation** — before the attempt and
again after — so a run killed mid-copy leaves `last_attempt` moved and `last_success` stale,
which is the signature to look for. `verify.ps1` section 6 reads it.

**The row's own open question was *whether* the inbound copier should carry the same record,
and the answer is yes with the row's caveat kept.** OBS-044 argued the inbound record is
quieter evidence than the outbound one, because Drive is a concurrent writer and `0 new` is
the expected state most of the time. That is true and it is written into section 6 rather
than discarded: **`last_attempt` is the field that carries information here**, and an
`outcome` reading `up to date` does not.

**Two record files, not one shared with the export**, reasoned at `RUN_RECORD` in
`tools/sync_from_drive.py`: the copiers are in two languages, so one file means two
implementations of one write-and-parse contract, and a Python read-modify-write around
PowerShell's whole-file `Set-Content` would put both copiers' evidence at risk in the one
artifact whose job is surviving a crash.

**What `037` could not have known, and 043 measured.** OBS-044 said the inbound copier had
*the same* defect. It had a worse one: **a destination that could not be created was not an
outcome at all** — `dest.mkdir()` raised `FileNotFoundError` and the tool died with a
traceback, exiting non-zero only because an unhandled exception happens to exit 1. Luck, not
a contract. It is now a named outcome. Pinned by `tests/test_sync_run_record.py`, whose
failure-path cases run the tool as a program and read the record afterwards.

**OBS-046 (the 2026-08-14 row — *worktrees outlived their tasks*) · PROMOTED.**
`resolution:` Became `043` Part 3. **`verify.ps1` section 7 prints the count, each worktree's
name, and its age in days**, with the age's basis named — the directory's creation time on
this disk. No verdict and no threshold: a three-day-old worktree may be a task in flight or a
task that finished on Tuesday, and the script does not know which. **It removes nothing**
(`OBS-036`: a verification script with a side effect cannot be run to find out whether
something happened).

The two worktrees the row was written about — `.claude/worktrees/024-subagent-roster` and
`.claude/worktrees/029-entry-point` — were confirmed clean by `git status --porcelain` and
removed under `043`, on Christoph's explicit confirmation that both were merged and safe.

**OBS-034's prediction is the part worth keeping.** It said the breakage was transient
because removing the worktree clears it. **Both halves were true and the conclusion was still
wrong**: removal does clear it, and nobody removed them, so it was red for three days. A
remedy that depends on someone remembering is not a remedy, which is the same argument the
adoption gate's test makes about prose conventions.

**OBS-064 · PROMOTED.**
`resolution:` Fixed in `043`, in the session that found it. The two `main()` calls in
`tests/test_sync_from_drive.py::test_main_returns_nonzero_when_a_person_must_look` now pass
`--record` into `tmp_path`, as does `test_an_unknown_pair_id_is_an_error_not_a_silent_no_op` —
the last on the principle that **the guard belongs on the call, not on the current control
flow**: that path returns before writing today, and a later edit moving the write earlier must
not silently re-arm it.

**Made structural rather than fixed by eye**, because reviewing every `main()` call by hand is
how it was missed the first time.
`test_no_test_in_this_file_writes_the_tracked_run_record` parses that file's own source and
fails on any `main([...])` literal without `--record`.

**Verified by measurement, not by reading.** `sync-run-record.md` was stamped with a real
`christoph_open` sync, the three sync suites were then run, and the record still carried the
real outcome afterwards — `git status` clean of it.

**A second-order cost this also removes**: the record is tracked, so every suite run left it
modified and the tree dirty — noise in the one signal `verify.ps1` section 2 exists to carry.

**Its unmeasured half is still open and is NOT covered here.** `OBS-064` also asks whether
`export-handoff.ps1` has the same hole through `-RunRecordOverride`. It was not measured, and
this resolution does not claim it was.

**OBS-014 · PROMOTED.**
`resolution:` Became `018` part 2. The per-tile check now re-evaluates on every resize via
`live/tui/app.py`'s `Frame.on_resize`, switching **in both directions**, and the refusal message
is recomputed rather than reused. Pinned by
`test_shrinking_after_launch_refuses_and_growing_back_restores` and
`test_the_refusal_message_reflects_the_current_size_not_the_launch_size`.

**OBS-015 · PROMOTED.**
`resolution:` Became `018` part 3. Rows 5 and 8 are named `select` and `submit` in
`config/layout.yaml`; `human: true` and the value cell are unchanged, so the distinction between
*a stage the system does not perform* and *one it has not performed yet* is intact. Pinned by
`test_every_pipeline_row_has_a_name`.

**OBS-016 · PROMOTED.**
`resolution:` Became `018` part 4. `manage` carries `deferred: "not core, revisit later"` and
renders `[ NOT BUILT ] (deferred - not core, revisit later)`. **No new badge word was invented**
— `[ DEFERRED ]` is not in `SPEC.md` §4's vocabulary and `grammar.py` states that adding one is
a spec change, so the ruling lives in the reason. **`slice not assigned` remains reachable** for
a stage declaring no claim at all, and `BUILD-PLAN.md` still contains no slice building position
management, which is OBS-006 and stays OPEN.

**OBS-013 · PROMOTED.**
`resolution:` Carried into the ledger as an unverified reading, and it had already been
settled by the time the row was written. `S009a` measured Christoph's terminal at
**209 × 54** with `$Host.UI.RawUI.WindowSize` — not derived from pixels — giving **67 columns
per tile against `BOX_WIDTH = 71`.** The reading was correct.

It is promoted rather than dropped because it **became a test**:
`live/tests/test_tui_measured_against_its_tile.py` pins `209 × 54` as the primary snapshot
width with that reasoning recorded beside it, and `test_no_line_ever_exceeds_the_width_it_was_given`
makes the underlying property — nothing renders at a width it was not measured against —
enforced rather than observed.

**Recorded here rather than silently corrected**, because the row's *kind* changed: it entered
as a reading and left as a measurement, and the fact that a design-session inference turned out
to be right is itself worth being able to find later.

**OBS-023 · DROPPED.**
`resolution:` **Resolved by cutting the row, prompt v1.7.** Strip row 2 — the consumer this
failure disabled — no longer exists, and veto 2 with it. The four sessions of evidence are kept
above because they are what the row recorded; what changed is that they describe **history**
rather than a live blindness.

**Corrected by `028` Part 1, and the reason it needed correcting is worth more than the row.**
`027` took its recurrence counts from the 2026-08-13 snapshot, which was produced under prompt
**v1.6**. The prompt had already moved to v1.7 and *the snapshot could not know it*. A count read
from an artifact is a count as of that artifact's world. **That is this project's first pattern —
a well-formed value answering a different question — and it was committed in a task file whose own
Part 1 warns about exactly that.**

**Not deleted, deliberately.** A row removed would lose the fact that four sessions of HYG
failures were once being tracked, which is the thing this ledger exists to stop.

**OBS-029 · DROPPED.**
`resolution:` **Nothing to watch.** Layer I row 1 was cut in prompt v1.7 and replaced by *credit,
prior session* from IBKR daily bars. `027` recorded HY OAS at **2 of 3** consecutive sessions and
asked that the count be held so rule 15's threshold would mean something; `028` retires the watch
entirely, because the consumer is gone.

**Entered as a DROPPED row rather than simply deleted from `027`'s instruction**, which is a small
deviation from `028`'s wording — it says *"delete this instruction entirely"*. The ledger's own
rule is that a finding leaves by `PROMOTED` or `DROPPED` **with the reason recorded**, never by
disappearing: *"deleting one silently loses the fact that someone once thought it mattered."*
A `DROPPED` row satisfies both — there is nothing open, and the count that was being held is
still findable by whoever wonders what happened to it.

**OBS-047 · PROMOTED.**
`resolution:` **Into `SPEC.md`, in place, under `038`.** §6b.1b-ATR's `atr_d14` row and §4.4's
ADR row both asserted that extended hours cannot alter a daily bar. Both are corrected, and
**the wrong text is struck through rather than deleted** so that a reader who remembers the old
rule can see that it changed instead of wondering which of two documents is current. The new
§4.4a.1 carries the replacement in full.

**Recorded as PROMOTED rather than simply fixed**, because the interesting part is not the
correction. §6b.1b had already written down the exact experiment that would have caught it —
*"pull the same daily series both ways on a symbol with a large pre-market gap and diff
high/low/volume. Cheap, and it settles the question permanently"* — and nobody ran it. **A spec
that names its own unverified assumption and the one-line test for it is doing everything short
of the thing that matters.** What closed it was a UAT looking at something else.

**OBS-051 · PROMOTED.**
`resolution:` **The levels half went into `SPEC.md` §4.4a.1 under `041`; the SMA half is
deliberately still open and is the only thing this row now covers.**

`041` ruled the thirteen remaining levels RTH — `HOD`/`LOD`, the prior week, the prior month,
and `52wH`/`52wL`/`ATH` — on a composition argument: a week's high must be the maximum of its
days' `PDH`s, and `038` made `PDH` RTH. `52wH`/`52wL` were the two this row named, and they are
settled.

**The SMA stack is not, and `041` declined to rule it on purpose:** *"ruling a value nothing
reads is admin."* It must be ruled **before anything consumes it**, and `SMA_BASIS.why` now says
so in the code rather than only here.

**One correction to `041`'s reasoning, recorded because the conclusion survived it and the next
reader should not have to re-derive this.** `041` argues this row's original point is void
because *"`ext` no longer exists"* — `ext 10/20/50` having left the panel under the scope
decision of 2026-08-14. **They have not left the tree.** They are still in `CONTEXT_ORDER`, still
computed by `extension_in_adr`, and still rendered, because that scope decision is a decision and
`S012` has not been built. **So the original argument still stands** — `ADR $` is RTH under
`038`, so an ETH SMA would divide across two bases — **and it points the same way `041` does.**
The ruling is unaffected; only the stated reason for it was wrong.
