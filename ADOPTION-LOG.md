# ADOPTION-LOG

One row per file adopted into this tree through the gate (`tools/adopt.py`).

`tests/test_adoption_log_complete.py` asserts every tracked file outside the bootstrap
allowlist appears here or in `EVIDENCE-CARRY.md`. A file that arrives by any other route
turns the suite red — that is the mechanism, and it is why this table is not documentation.

Evidence does **not** appear here. It is carried, not adopted, and lives in
`EVIDENCE-CARRY.md`.

| date | path in new tree | source path | origin | reason | test that covers it | adopted by |
|---|---|---|---|---|---|---|
| 2026-08-24 | `live/tests/test_088_adr_day_boundary.py` | `authored in this tree under 088; not imported` | authored | 088's exit tests: Green ×2 (`rth_dailies[-1]` dated today computes, on the one `ADR_BASIS` object throughout — Part 0 item 3, asserted by `is`; `today_et==""` preserves the exact pre-088 behaviour, the deliberate escape hatch), Refusal ×3 (the 04:00 reproduction refuses with `session not started`; the rendered row asserted exactly, not by substring — B-126; the three states — computed / day-boundary refusal / fetch-failure refusal — pairwise distinct), Boundary (one `Stage2Inputs`, only `rth_dailies[-1]`'s own date crosses the line — refuses one bar short of today, computes the instant it is dated today), Fixture (AST import-inspection, no state borrowed from `test_attach.py`/`test_080_two_stage_attach.py`). No Divergence test — Part 0 item 3 found no second basis object to diverge from the first (Candidate B does not hold), so there was nothing for an identity guard to check. Confirmed RED via `git stash` of `live/attach/attach.py`/`live/tui/app.py` against the real pre-088 tree (4 of 7 tests failed with `TypeError: Stage2Inputs.__init__() got an unexpected keyword argument 'today_et'`) — see the done-note. | `is a test` | Claude Code (088) |
| 2026-08-24 | `live/tests/test_087_repaint_path.py` | `authored in this tree under 087; not imported` | authored | 087's seven exit categories: Green ×3 (a landed value updates in place, not a full remount — B-141, spying `Frame.remove_children` directly; the age advances with no stream pushing at all — B-140, read from RENDERED TEXT at two real points in time, never `header_freshness()` called directly; the age climbs 1s/2s/3s/4s over four real seconds), Refusal ×4 (a dead HEALTH stream is marked stale at the same 20s rule the header uses; a borderline-fresh stream is not; a row pending past its bound becomes a named, unfitted refusal; a row within its bound still reads plain `pending`, and one with no attach clock at all does too — not a false refusal), Teardown (`_begin_attach`'s cancel loop fires once per stream N attaches of the same symbol actually opened), Colour ×2, Fixture (AST import-inspection, no state borrowed from any other file's shared fixture). The two Green age tests genuinely spend real wall-clock time (~4s each) — reading rendered text at real time points is the whole point, since the bug was that correct arithmetic was never painted. All four fix-specific behaviours confirmed RED via a targeted, temporary revert of each fix in turn (not `git stash`, since two concurrent peer sessions were actively editing `attach.py`/`app.py` for an unrelated task, 088, mid-session) — see the done-note. | `is a test` | Claude Code (087) |
| 2026-08-24 | `live/tui/pending_config.py` | `authored in this tree under 087; not imported` | authored | `load_pending_timeout_s()` — the one place `config/pending.yaml` is read, same discipline `live/attach/rvol_config.py` already established for `rvol_basis` (every setting required, none defaulted, a `note:` mandatory). Deliberately in `live/tui/`, not `live/attach/` — the bound governs when a row STOPS rendering `pending`, a rendering concern, not an input to `compute_context_and_rail`'s own arithmetic. | `live/tests/test_087_repaint_path.py` (indirectly, via `Stage2Inputs.pending_timeout_s`/`Attached.pending_timeout_s`) | Claude Code (087) |
| 2026-08-24 | `config/pending.yaml` | `authored in this tree under 087; not imported` | authored | `pending_timeout_s` — how long a value row may sit `pending` before it renders a named refusal instead. UNFITTED: a ceiling (90s) chosen deliberately past every live wall time 082 measured (60s+), far short of the hundred-minute stall Christoph observed that triggered this task — not a fit from data, Christoph's ruling is owed if it needs recalibrating. | `live/tui/pending_config.py` (loader validation); `live/tests/test_087_repaint_path.py` (behaviour, via direct `Attached` construction rather than a live config read) | Claude Code (087) |
| 2026-08-24 | `live/tests/test_084_rvol_curve_cache.py` | `authored in this tree under 084; not imported` | authored | 084's five exit categories: Green (a second attach of the same symbol issues no curve request and renders the same value), Identity (the important one — `_MD` answers a HIGHER-volume curve on every call it actually receives, so a passing "second value equals first" assertion proves the cache served the exact object the first landing stored, not a coincidence of a static fixture agreeing with itself), Key isolation ×3 (changing the anchor misses; changing the session date misses; two symbols sharing a sector ETF hit on the shared sector curve while the own curve stays keyed per-symbol), Refusal (a failed fetch never populates the cache, and the next attach still tries rather than being permanently blocked by a phantom entry), plus a direct LRU-bound test and the same Fixture AST check 083 introduced (no state borrowed from `test_attach.py`/`test_attach_is_reachable_by_key.py`). Confirmed RED via `git stash` of `live/attach/attach.py`/`live/tui/app.py` and this task's own test-helper fixes against the real pre-084 tree (`ImportError: cannot import name 'RVOL_CURVE_CACHE_MAX'`) — see the done-note. | `is a test` | Claude Code (084) |
| 2026-08-24 | `live/tests/test_083_rvol_anchor.py` | `authored in this tree under 083; not imported` | authored | 083's five exit tests: Green (the anchor changes the request and the render together, the 59% smaller RTH curve confirmed by construction), Divergence (the B-049 guard — asserts the curve-request call site and the numerator-filtering call site read the SAME `Stage2Inputs.rvol_basis` object, by identity, not merely equal values; a second mutation test confirms both halves move together when the one object changes), Derived label (the rendered word is never a literal), Refusal (the anchor renders while pending and while unavailable alike), Fixture (checked by AST import-inspection that no state here is borrowed from `test_attach.py`'s shared `Fake`). Confirmed RED via `git stash` of `attach.py`/`ibkr.py`/`app.py`/`day_record.py` against the real pre-083 tree (`ModuleNotFoundError` on `live.attach.rvol_config`) — see the done-note. | `is a test` | Claude Code (083) |
| 2026-08-24 | `live/attach/rvol_config.py` | `authored in this tree under 083; not imported` | authored | `load_rvol_basis()` — the one place `config/rvol.yaml` is read and turned into a `SessionBasis`, so `core/indicators/context.py` (deliberately pure — no file I/O) never has to touch a file itself. `anchor_word(basis)` derives the rendered `rth`/`eth` word from `basis.use_rth`, never a literal at any call site. | `live/tests/test_083_rvol_anchor.py` | Claude Code (083) |
| 2026-08-24 | `config/rvol.yaml` | `authored in this tree under 083; not imported` | authored | `rvol_anchor` — the one key both halves of the RVOL ratio read (`rth`/`eth`, default `rth`). Corrects project instructions §8 ("a basis is never config"): RVOL's basis is genuinely a choice, self-constrained only by needing to match itself, unlike ADR%/ATR/the opening range, which are fixed by arithmetic and stay module-level constants in `core/indicators/context.py`, untouched by this file. Default RTH is reasoned from Nasdaq's Global Trading Hours launch (2026-12-06) and from the ~59% smaller request it produces, not fitted from live use — mockup v1.6 §11 lists it as an open claim. | `live/tests/test_083_rvol_anchor.py::test_part0_the_config_default_is_rth` | Claude Code (083) |
| 2026-08-23 | `live/tests/test_080_two_stage_attach.py` | `authored in this tree under 080; not imported` | authored | 080's own four exit tests (Green/Refusal/Colour/Fixture) plus HEALTH-metrics coverage: rows land independently (asserted by feeding `compute_context_and_rail` one more input at a time), the header freshness age advances, `RVOL` renders both labelled readings in the exact ruled wording, four refusal states never render alike, `stale` never appears off a fresh stream and appears exactly once when one stream ages past threshold, and Part 4's measurements never leak onto ATTACHED. Every state is constructed locally — checked by import inspection — never borrowed from `test_attach.py`'s shared `Fake`, per 078's own fixture-masking lesson. Confirmed RED via `git stash` of `live/attach/attach.py`/`ibkr.py`/`live/tui/app.py`/`day_record.py` against the real pre-080 tree (`Stage2Inputs` import failure) — see the done-note. | `is a test` | Claude Code (080) |
| 2026-08-23 | `live/attach/streaming.py` | `authored in this tree under 080; not imported` | authored | `StreamHandle` — the one shared primitive `open_price_stream` returns, used identically by the `MarketData` Protocol, `IBKRMarketData` and every fixture. Idempotent `cancel()`, so the caller that owns a handle (`app.py`, on a symbol switch) and the stream's own error path can both reach for it without either having to check which got there first. | `live/tests/test_080_two_stage_attach.py` (indirectly, via `compute_context_and_rail`); the type itself has no arithmetic to test | Claude Code (080) |
| 2026-08-23 | `tests/test_verify_failures_state_is_ignored.py` | `authored in this tree under 068; not imported` | authored | `verify.ps1`'s second exemption to writing nothing but its own output file (`verify-failures.txt`, the failure-delta state — 068 Part A) is gitignored the same way `handoff/verify-output.md` is; this pins it with `git check-ignore` rather than a `.gitignore` substring match, mirroring `test_verify_output_is_ignored.py`. | `is a test` | Claude Code (068) |
| 2026-08-23 | `live/tests/test_071_four_row_context_block.py` | `authored in this tree under 071; not imported` | authored | 071 Part 1's five snapshot fixtures, pinned at the panel's real tile width (`209 // 3 - TILE_PADDING`), against `ATTACHED mockup — the context block and its states` v1.2: the landed panel is exactly four rows, no `from`/`slot`/`tape`/level rows/VWAP continuation, `ADR% used` renders the compact `of $X ADR20 RTH` form, and the partial-count line carries no tilde. Each fixture (bar the one already-green no-op case) confirmed RED via `git stash` of the real pre-071 `live/tui/app.py`/`core/indicators/context.py`, not a synthetic double — see the done-note. | `is a test` | Claude Code (071) |
| 2026-08-23 | `live/tests/test_072_attach_switches_symbol.py` | `authored in this tree under 072; not imported` | authored | 072's four required tests plus a mid-flight fixture: attaching a second, DIFFERENT symbol used to accumulate in `record.attached` rather than replace it (`_begin_attach`/`_finish_attach` only ever cleared an entry matching the SAME symbol) — QQQ never left the screen and AMZN's own block grew below it, reproduced live before any fix. `TwoSymbolFake` resolves each symbol to its own contract, unlike `test_attach.py`'s `Fake`, whose `resolve()` ignores its argument — the reason nothing had caught this before. All five confirmed RED via `git stash` of the real pre-072 `live/tui/app.py`, including one that reproduces the reported behaviour byte-for-byte mid-flight. | `is a test` | Claude Code (072) |
| 2026-08-23 | `live/tests/test_078_surface_the_silent_degrade.py` | `authored in this tree under 078; not imported` | authored | 078's three required exit tests (Green/Refusal/Guard) plus a control: a `warm()` failure that every row still measures through (fallback succeeds) used to leave `AttachResult.partial` empty — a fully successful-looking attach that had actually taken the unguarded, pre-058 sequential path throughout, exactly as `075` measured live on 3 of 6 AMZN attaches. `IBKRMarketData._bars`, the single choke point every fallback request goes through, never consulted `_PacingGuard`. Four of five confirmed RED via `git stash` of the real pre-078 `attach.py`/`ibkr.py` (the fifth is a green-baseline control, correctly unaffected). `FakeWithPremarket` extends `test_attach.py`'s `Fake` to give PMH/PML real pre-market bars — the shared `Fake`'s own fixture never does, so no existing test could reach a genuinely CLEAN (zero-refusal) attach to isolate the degraded-gather signal from an unrelated ordinary one. | `is a test` | Claude Code (078) |
| 2026-08-23 | `tools/exported_notes.py` | `authored in this tree under 068; not imported` | authored | Which `handoff/done/` notes have already been exported, read from the export manifest rather than inferred from a number or a date — 068 Part C. Extracted rather than embedded in `tests/test_donenote_bugs_block.py` or `verify.ps1`, so both read the same answer to "is this note out of scope for the `bugs:` guard", the same reasoning `tools/waiting.py` and `tools/now.py` already follow. Fails closed: raises `ExportManifestUnreadable` rather than returning an empty set on any failure to locate or parse the manifest. | `tests/test_donenote_bugs_block.py::test_an_unreadable_manifest_refuses_by_name_rather_than_exempting_everyone`, `test_058_is_actually_exported_and_that_is_what_makes_the_exemption_real` | Claude Code (068) |
| 2026-08-15 | `live/tests/test_attaching_state.py` | `authored in this tree under 058; not imported` | authored | **Logged retrospectively by 068.** 058 Part 3's product half: every value dependent on the outgoing symbol drops immediately on attach, one `[ ATTACHING SYMBOL ]` badge renders while the gather is in flight, and every value lands in one paint when it completes. Authored under 058 and never entered here, so `test_adoption_log_complete` was red on it since 058 landed — recorded rather than allowlisted, the same shape `test_worktree_root_is_scanned.py`'s row below already used. | `is a test` | Claude Code (058), logged by 068 |
| 2026-08-15 | `live/tests/test_pacing_guard.py` | `authored in this tree under 058; not imported` | authored | **Logged retrospectively by 068.** 058 Part 2's guard: IBKR's own documented pacing limit (six or more historical requests for the same Contract/Exchange/TickType inside two seconds) built and seen red first — the test that fires the guard at exactly the fifth-and-sixth request the task names, constructed so removing either of the last two calls passes it for the wrong reason (headroom, not correctness). Same retrospective-logging gap as its sibling row above. | `is a test` | Claude Code (058), logged by 068 |
| 2026-08-22 | `tests/test_permission_policy.py` | `authored in this tree under 061; not imported` | authored | The permission policy must be carried by a test, not by memory. Asserts the property — `permissions.deny` holds a `Write`/`Edit` entry covering `.claude/settings.json` itself — not the exact two strings Christoph pasted. Proven against scratch copies of the real file's content, never against the real file: seen red with the two lines removed, green with them present, and refuses cleanly on malformed JSON and on a deny entry naming a verb that is not a real tool. | `is a test` | Claude Code (061) |
| 2026-08-16 | `tests/test_question_file_shape.py` | `authored in this tree under 054; not imported` | authored | 054 Part 3 / 043 Part 2. A question file's minimum shape -- valid frontmatter `status:`, `**Raised by**` and `**Blocks**` body lines -- enforced as a mechanism rather than left as the example text in a task file. Deliberately checks the frontmatter `status:` key rather than requiring a body `**Status**` line, which would collide with CLAUDE.md's five-state handoff vocabulary already using that exact bold word for a different thing. | `is a test` | Claude Code (054) |
| 2026-08-16 | `config/outputs.yaml` | `authored in this tree under 053; not imported` | authored | **The declared list of artifacts the protocol owes the design session.** Prose cannot be tested; a list can. Exists because `verify-output.txt` sat at the repository root from `023` to `053` while `REVIEWED` named it as the evidence — **written where nothing could carry it**, silently, because the file appears and the script exits 0. Carries an `exclusions:` section so a considered omission is distinguishable from an oversight. | `tests/test_declared_outputs_are_exported.py` | Claude Code (053) |
| 2026-08-16 | `tests/test_declared_outputs_are_exported.py` | `authored in this tree under 053; not imported` | authored | 053 test 1. **The general form of the defect, not the instance** — a one-line path change fixes `verify-output.txt`; this asks *how many more of these are there* on every run. **Export scope is derived from `export-handoff.ps1`, never restated**, so a pair added there changes what this allows without anyone remembering. | `is a test` | Claude Code (053) |
| 2026-08-16 | `tests/test_donenote_bugs_block.py` | `authored in this tree under 053; not imported` | authored | 053 test 2. A finding reaches the tracker as data instead of being retyped — all 88 rows exist because somebody typed them. **`bugs: []` and a missing block must not look alike.** Watermarked at task 053: retrofitting `bugs:` onto forty historical notes would mean inventing findings nobody recorded. | `is a test` | Claude Code (053) |
| 2026-08-16 | `tests/test_task_file_shape.py` | `authored in this tree under 053; not imported` | authored | 053 test 3. Addressing gate, `class`, `unblocks:` on admin, and **no destination key** — routing is protocol, not task content. **Frontmatter only, deliberately**: 053 forbids grepping prose, since a check that catches four phrasings and misses the fifth is worse than none because it would be trusted. | `is a test` | Claude Code (053) |
| 2026-08-16 | `tests/test_inbound_run_record_has_no_conflicts.py` | `authored in this tree under 053; not imported` | authored | 053 test 5. The inbound copier's refusal is correct; **a refusal nobody clears stops carrying information.** `040` and `043` have differed since `045` and became furniture inside the copier's own output. **No allowlist** — green means somebody ruled, and ruling is never overwriting. | `is a test` | Claude Code (053) |
| 2026-08-16 | `tests/test_one_task_id_one_file.py` | `authored in this tree under 053; not imported` | authored | 053 test 6, B-027. One number, one file, **pinned to the gate's own glob `NNN-*.md`**. The looser rule — leading digits alone — reported `008`, `012`, `013` and `035` as clashes, and **all four are correctly named files using the letter-suffix convention**. A test one character too loose would have demanded four needless renames. | `is a test` | Claude Code (053) |
| 2026-08-16 | `tests/test_worktree_root_is_scanned.py` | `authored in this tree under 052; not imported` | authored | **Logged retrospectively by 053.** Authored under 052 when worktrees moved out of the repository and the scan moved with them, and never entered here — so `test_adoption_log_complete` was red on it before 053 started. **Recorded rather than allowlisted**: the gate working on its own author is the gate working. | `is a test` | Claude Code (052), logged by 053 |
| 2026-08-15 | `sync.ps1` | `authored in this tree under 045; not imported` | authored | **The command Christoph did not have.** The inbound copier had no trigger of its own and no document stated its invocation, so four UAT files sat in Drive while `christoph/open/` held one `.gitkeep` — and the one party who could have unblocked it was not told how. One word, at the root, beside `verify.ps1`. It wraps and does not reimplement: every rule stays in the Python copier. | `tests/test_sync_run_record.py`, `tests/test_waiting_in_drive.py` | Claude Code (045) |
| 2026-08-15 | `tools/now.py` | `authored in this tree under 045; not imported` | authored | `claude/NOW.md`, computed from the tree — what is runnable, what is blocked and on what, what is superseded, what sits with Christoph. **Derived and never stored**, because three sessions write here and a hand-maintained board is wrong within the hour and believed anyway. | `tests/test_now_is_derived.py` | Claude Code (045) |
| 2026-08-15 | `tools/waiting.py` | `authored in this tree under 045; not imported` | authored | Counts what Drive holds that the tree lacks. **Extracted from `verify.ps1` rather than embedded in it, so it can be tested by running it** — 045: *a static check would pass forever against a `verify.ps1` that had stopped counting.* An unreachable source is its own outcome and never reads as `0 waiting`. | `tests/test_waiting_in_drive.py` | Claude Code (045) |
| 2026-08-15 | `tools/register-sync-task.ps1` | `authored in this tree under 045; not imported` | authored | Registers the 15-minute inbound sync as a Windows Scheduled Task. **Run by Christoph, not by a session** — it writes outside the repository. Overturns `037`'s ruling against a daemon on the grounds that `037` objected to *silence*, and `043`'s run record removed it. | `n/a — registers a machine-level task; the sync it runs is covered by tests/test_sync_run_record.py` | Claude Code (045) |
| 2026-08-15 | `tests/test_now_is_derived.py` | `authored in this tree under 045; not imported` | authored | 045's tests 3 and 4. **The hand-edit case goes through `main()`, not `render()`** — the first cut asserted only that the renderer omits the edit and never reached the code deciding whether to write, and a mutation proved it blind. | `is a test` | Claude Code (045) |
| 2026-08-15 | `tests/test_waiting_in_drive.py` | `authored in this tree under 045; not imported` | authored | 045's test 2, behavioural: real source folder, real destination, the counter run over them. Includes the assertion a behavioural test cannot make of itself — that `verify.ps1` still invokes the counter at all. | `is a test` | Claude Code (045) |
| 2026-08-15 | `live/tui/links.py` | `authored in this tree under 044; not imported` | authored | `SPEC.md` §4.1a's primitive and its guard. **A link is a claim about two things and never appears alone** — that invariant is the entire reason link colour is allowed to exist beside §4.1's no-verdict rule, so it is enforced in code rather than described. Nothing renders a link yet; `S012` builds the rail. | `live/tests/test_link_colour_never_appears_alone.py` | Claude Code (044) |
| 2026-08-15 | `live/tests/test_link_colour_never_appears_alone.py` | `authored in this tree under 044; not imported` | authored | 044 Part 1's test. Exercised against **constructed** lines as well as the live panels, because nothing emits a linked token today and a guard that has only ever run over zero tokens has not been shown to fire. | `is a test` | Claude Code (044) |
| 2026-08-15 | `core/tests/test_classification_boundaries_are_exact.py` | `authored in this tree under 044; not imported` | authored | 044 Part 2's five boundary cases, **constructed from prices rather than from a literal `R_closed`** — the defect lives in the division, so the division has to happen inside the test's subject. That is how the original got past a suite of 35 tests. | `is a test` | Claude Code (044) |
| 2026-08-15 | `tests/test_observation_ids_are_unique.py` | `authored in this tree under 044; not imported` | authored | 044 Part 3's id-uniqueness assertion — the cheap half, and it should have existed before any of this. **It is currently RED on purpose**: the reallocation it would clear is stopped pending a ruling, and `xfail` was rejected because it removes the row from the failure count. | `is a test` | Claude Code (044) |
| 2026-08-15 | `core/indicators/context.py` (amended) | `authored in this tree under 038; not imported` | authored | **Not a new file — recorded because 038 adds the `SessionBasis` type and six basis constants to it**, which is the ruling the rest of the task hangs off. `SPEC.md` §4.4 puts every setting in `config/`; a session basis is exempt because it is a fact about what an indicator is rather than a choice, and the exemption is stated in the module so it cannot be undone by someone tidying toward consistency. | `tests/test_session_basis.py` | Claude Code (038) |
| 2026-08-15 | `live/tui/numbers.py` | `authored in this tree under 038; not imported` | authored | The one number formatter `SPEC.md` §4.0a has required since it was written and which did not exist: `live/tui/app.py` rendered every context value through a hardcoded `f"{value:,.2f}"`, so `ADR used` read `67.00`, `round` read `47.00` and a share count read `18,119,366.00`. Also carries the refusal for a value that arrives with no declared basis. | `live/tests/test_rendered_rows_declare_basis_and_unit.py` | Claude Code (038) |
| 2026-08-15 | `config/formatting.yaml` | `authored in this tree under 038; not imported` | authored | §4.0a's precision table, moved out of the renderer into config where §4.4 says settings live. **Deliberately the mirror image of the basis constants**: precision is a choice somebody could sensibly make differently, a session basis is not, and the two are separated so that neither argument gets applied to the other. | `live/tests/test_rendered_rows_declare_basis_and_unit.py::test_the_formatter_matches_spec_4_0a` | Claude Code (038) |
| 2026-08-15 | `tests/test_session_basis.py` | `authored in this tree under 038; not imported` | authored | 038 Part 5 tests 1 and 2. Asserts the flag on the wire equals the flag on the constant — **not that the constant exists**, which is the assertion that would have passed on the broken code — and that ADR and ATR request different flags, because a configuration where they agree makes test 1 vacuous. | `is a test` | Claude Code (038) |
| 2026-08-15 | `live/tests/test_rendered_rows_declare_basis_and_unit.py` | `authored in this tree under 038; not imported` | authored | 038 Part 5 tests 3 and 4, scoped positionally to the render layer. Asserts every rendered number names a session basis or a clock anchor and carries a unit. | `is a test` | Claude Code (038) |
| 2026-08-15 | `live/tests/test_qqq_2026_08_13_regression.py` | `authored in this tree under 038; from Christoph's 013 UAT against IBKR's own TWS charts, 2026-08-13` | authored | 038 Part 5 test 5. **The first externally-checked values this project has** — PDH, PMH, PML, ORH, ORL verified against IBKR's own daily bars, and ATR14 against TWS's daily ATR(14). Bar fixtures only, no network. Pins that `PDL` is the regular-session low and **not** the 717.37 extended-hours low that `036` would have baked in. | `is a test` | Claude Code (038) |
| 2026-08-11 | `docs/specs/CHRISTOPH-TASKS.md` | `design session (Claude chat), 2026-08-11, in conversation with Christoph` | authored | Items requiring Christoph's own action — UATs and external enquiries — have had no home. They have been raised in chat and lost at the end of the session. Placing them in handoff/inbox/ is unsafe because Claude Code executes that folder on "Do inbox NNN", so a task addressed to a human would be executed by a machine that was not asked to judge it. | `n/a (not a code tree)` | Christoph |
| 2026-08-11 | `docs/specs/HANDOFF-PROTOCOL.md` | `authored in the design session of 2026-08-11; not imported, not derived from momentum-harness, not carried from Drive` | authored | first written statement of the handoff convention, which has run unwritten for the whole project and has already produced one task treated as finished on a report the design session could not see | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/REGIME-PROMPT.md` | `supplied by Christoph, 2026-08-10, as the v1.2 revision of the scheduled regime prompt` | authored | v1.2 closes H8's two open items — the absent ratification bands and the bare `6 of 9` denominator example. Both were live defects in a CURRENT document running daily at 05:00. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/mockups/mockup-README.md` | `momentum-harness/docs/specs/mockups/mockup-README.md` | authored | Index for the mockup set; explains what each screen is and is not. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/mockups/mockup-05-live-context.html` | `momentum-harness/docs/specs/mockups/mockup-05-live-context.html` | authored | Live-context mockup including the NOT BUILT state, cited by task 005. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/mockups/mockup-04-size-stage.html` | `momentum-harness/docs/specs/mockups/mockup-04-size-stage.html` | authored | Sizing and staging screen mockup. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/mockups/mockup-03-watchlist.html` | `momentum-harness/docs/specs/mockups/mockup-03-watchlist.html` | authored | Ranked watchlist mockup, the visual contract for task 006. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/mockups/mockup-02-regime.html` | `momentum-harness/docs/specs/mockups/mockup-02-regime.html` | authored | Regime screen mockup, cited directly by task 005 as a source spec. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/mockups/mockup-01-ingest.html` | `momentum-harness/docs/specs/mockups/mockup-01-ingest.html` | authored | Ingest screen mockup; the visual contract for watchlist ingestion. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/layer0-amendment-2-frozen-vs-live.md` | `momentum-harness/docs/specs/layer0-amendment-2-frozen-vs-live.md` | authored | Amendment 2 to the Layer 0 spec, the frozen-vs-live split. Task 003 in handoff/done implements it. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/USE_GUIDE.md` | `momentum-harness/docs/specs/USE_GUIDE.md` | authored | Operating guide for the harness. Adopted with its own caveat recorded: PROVENANCE.md gives it refs=0, so nothing in the old tree referenced it. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/REPO_CONSOLIDATION_PLAN.md` | `momentum-harness/docs/specs/REPO_CONSOLIDATION_PLAN.md` | authored | The plan the consolidation actually followed, and the definition of 'step 7' that M001 §6 and H9a §3b both turn on. Referenced by 3 files in the old tree. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/DRIVE-ARCHIVE-LIST.md` | `supplied by Christoph from Drive, 2026-08-10 (Drive sync removed 2026-08-09)` | authored | The record of what remains in Drive and what could not be archived. With Drive sync removed on 2026-08-09 this is the only on-disk index of those documents. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/REGIME-PROMPT.md` | `supplied by Christoph from Drive, 2026-08-10 (Drive sync removed 2026-08-09)` | authored | v1.1, the prompt behind the scheduled Layer 1 task. Verified to carry 'PART E -- the three outputs' and the E0 subsection, so it is v1.1 and not the v1.0 H9 says to reject. | `n/a (not a code tree)` | Christoph — **SUPERSEDED** 2026-08-10 |
| 2026-08-10 | `docs/specs/BUILD-PLAN.md` | `supplied by Christoph from Drive, 2026-08-10 (Drive sync removed 2026-08-09)` | authored | Companion to SPEC.md and the source of the slice numbering. M001 could not report the slice-008 collision properly because this document was unreadable from the repo. | `n/a (not a code tree)` | Christoph |
| 2026-08-10 | `docs/specs/SPEC.md` | `supplied by Christoph from Drive, 2026-08-10 (Drive sync removed 2026-08-09)` | authored | The location of record for the system specification. A spec that lives only in Drive is invisible to the side that builds -- Layer 0 was fully specified and never built for exactly that reason. H9 exists to end that. | `n/a (not a code tree)` | Christoph |

## The first thirteen rows — H9, 2026-08-10

All thirteen are documentation, all `authored`, all adopted into `docs/specs/`. They split
into two populations that are cited differently, and the difference is the point:

- **Four canonical specs** — `SPEC.md`, `BUILD-PLAN.md`, `REGIME-PROMPT.md`,
  `DRIVE-ARCHIVE-LIST.md` — have **no `PROVENANCE.md` row**, because they have never existed
  in `momentum-harness`. H9a inventoried that repository; these came from Drive, supplied
  directly. "No row, and here is why" is the honest citation; inventing one would be worse
  than the absence.
- **Nine carried forward** — three specs and six mockups — have rows in `PROVENANCE.md`, all
  `authored`, and every one is **byte-identical** to the old repo's copy.

Before this, the table was empty for a reason worth keeping on the record: H9a had not run,
refusal 4 keys on the origin classification it produces, and adopting anything would have
meant inventing the value the refusal exists to demand. H9a ran first. That is the order the
gate is built to enforce.
